# Claude session data
.claude/

# Stray files
*.pdf

# OS files
.DS_Store
Thumbs.db

# Node
node_modules/

# Capacitor — generated at build time by Codemagic
www/
/ios/
/android/

# Original RGB-only avatar PNGs (backed up before alpha-channel fix)
avatar-originals-rgb/

# Duel sim scaffolding — JWTs + per-run outputs MUST NEVER be committed.
# Only sims/scripts/, sims/sql/, sims/README.md are version-controlled.
sims/.secrets/
sims/runs/

# Temp design extraction (Claude Design handoff bundles)
.design-tmp/

# Playwright e2e smoke artifacts (traces / videos / reports)
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Original full-length Suno generations (processed loops live in assets/audio/)
audio-originals/
art-originals/

# Local strategy doc — revenue numbers, never push
AWAKENED_GROWTH_PLAYBOOK.md

# Local working notes / sprint scratch — keep out of the repo
GROWTH_RESEARCH.md
GUEST_PATH_TEST_PLAN.md
SPRINT-W397-W402.md

# W907 — wrangler run from the repo ROOT autoconfigures a stray wrangler.jsonc here that
# then hijacks deploys from backend/. Never commit one; always run wrangler from backend/.
wrangler.jsonc
