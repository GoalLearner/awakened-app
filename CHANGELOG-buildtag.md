# APP_BUILD_TAG inline changelog (extracted W659)

This was a ~114KB inline comment on the APP_BUILD_TAG line in app.js, moved
here to cut parse weight on device. Historical prose only — the runtime value
is the quoted tag string.

W678 — The Threefold Court gets its OWN 3-piece drop pool (was borrowing the
Coursing Dread's): vestments_of_the_final_verdict (ultra_rare, body, INT — the
FIRST mage ultra at C in any slot), courthunters_grips (rare, gloves, FOCUS —
first ranger gloves at C), seal_of_the_threefold_court (rare, amulet, INT —
first mage amulet at C). Budgets on the W474 curve (rare Σ14 / ultra Σ22). New
'threefold_court' 3pc set (2pc +3INT+2VIT, 3pc +4INT+3FOCUS+2VIT, cp9 — below
the Choir/Wardens 4pc cp11 per the W506 ceiling rule). coopOnly BOSSES stub w/
dropTable {ultra .25, rare .55}; dropSourceBoss flipped to its own id;
process-boss-art JOBS pre-staged for 5 PNGs (2 boss + 3 items; art pending —
nano-banana prompts issued). Client-only (drops are per-device; no worker change).

---

W634 — retire the orphaned leaderboard metrics (owner: "we got rid of flights climbed, sleep streak, bedtime streak" + confirmed workout_streak + rank_band;
client-only). All 5 had NO live entry point (the metric switch is Steps·Highest Floor·Collection;
the Rankings Hub + renderLeaderboardPreview card list are dead/uncalled) yet lbSubmitAllMetrics kept SUBMITTING sleep/bedtime/workout/flights every sync = dead writes to invisible boards. Removed: the 5 submit pushes (metrics array now just step_total + the flag-gated floor_best/relics), LB_METRIC_META entries (sleep/workout/flights/rank_band;
bedtime already had none), LB_CLIENT_CAPS (sleep/bedtime/workout/flights), flights from LB_WEEKLY_METRICS, sleep_streak from _LB_MIN_QUALIFYING_SCORE. KEPT: the snapshot fields (snap.current_sleep_streak / flights_this_week / current_workout_streak — the Sleep/Workout HABITS + streak displays read them) + the two inert LEADERBOARD_WORKOUT/FLIGHTS_BACKEND_ENABLED consts + the dead renderLeaderboardPreview/hub shells (they thread through the sim-merge render at 41242 / _LB_CLIENT_ONLY_METRICS — removing them risks the LIVE boards for ~0 benefit;
unreachable so harmless;
a careful follow-up can delete the whole dead hub). Backend metrics.ts entries left (owner chose client-only;
no redeploy). Verified in preview: 3 live boards render, submit carries ONLY step_total/floor_best/relics_collected, no errors. Prior: W633 — profile-card open lag (owner: laggy, scale worry). _openProfileCard awaited the profile fetch THEN serially awaited Auth.fetchFriends() (GET /v1/friends, an N+1 = 1+F sequential D1 reads) before rendering — purely to gate the optional "Spar their Echo" button. So open latency = profile + entire friends fetch, growing with friend count (cold-cache first open was the laggy one). Fix: render immediately off the profile fetch;
warm _friendsCache in the BACKGROUND (fire-and-forget);
repaint ONCE only if the hunter is an accepted friend AND the card is still theirs (_pcOpenAlias guard prevents a late repaint of a card the user switched away from). Verified in preview: card renders in 66ms and does NOT wait for a mocked 3s friends fetch. Prior: W632 — Collection Log board empty despite the user having relics: root cause = relics_collected submits ONLY via the full lbSubmitAllMetrics batch, and NO board-open triggered that batch (only flights_climbed force-submits on open, app.js ~41994). The debounced cold-launch trigger was throttled out by targeted step/sleep submits (D1 evidence: metrics have scattered updated_at, relics_collected row never existed for any user). Fix: openLeaderboardRanking now force-submits (non-debounced, awaited) when metric==='relics_collected', mirroring the flights path — so opening the Collection board lands the caller's current relic count BEFORE the top fetch. Verified in preview (seeded non-zero steps): opening the board fires step_total+sleep+bedtime+workout+flights+floor_best+RELICS_COLLECTED. Backend also redeployed (Worker 8e69ad24) to guarantee relics_collected is in the live isValidMetric registry. Prior: W631 — leader-card eyebrow: tab context only on tabbed boards. _lbCurrentTab defaults to 'this-week' on every sheet open, so untabbed all-time boards (floor_best, relics_collected) wrongly read "★ LEADER · THIS WEEK" (spotted on the new Collection board;
floor_best had it latently since W386). Now the suffix only applies when LB_HOF_METRICS.has(metric);
untabbed boards read the plain "★ LEADER". Verified in preview: Collection + Highest Floor = "★ LEADER", Steps keeps "★ LEADER · THIS WEEK". Prior: W630 — Collection Log board reaches its first LIVE entry point. Owner: "where is the collection log leaderboard?" — W622 wired the board's rendering + metric + submit correctly, but its ONLY entry was a row in the legacy Global Rankings Hub (_lbHubRender/#lb-hub-sheet), a DEAD surface no longer reachable from the current UI (same stale-path failure class as W599's guild-invite buttons;
the live board is the #lb-rank-sheet with the W274 metric switch). Fix: third button "Collection" (data-lb-metric-switch=relics_collected) on #lb-rank-metric-switch (index.html), the W274 pair gate widened to the trio in openLeaderboardRanking (the generic switch click handler needed no change), .lb-metric-switch font 0.86→0.8rem + btn padding 10→6px + nowrap so three buttons fit at 375px. relics_collected isn't in LB_HOF_METRICS → sub-tabs auto-hide, title reads "Collection Log" (verified in preview: 3 buttons, no overflow, tap → correct board;
dead hub row left as-is, inert). Prior: W629 — Wardrobe shop tile name alignment (owner TestFlight report on 393). Two-line names ("Stardust Sovereign", "The Verdant Oracle") rendered left-ragged and pushed their rarity label lower than 1-line neighbors: the span box centers under the tile's flex column but wrapped TEXT lines keep default left alignment, and tiles had no reserved name height. Fix (styles.css only): .wd-tile-name gets text-align:center + line-height:1.25;
.wd-tile--premium .wd-tile-name gets a constant 2-line box (min-height:23px, flex-centered) so every rarity label starts at the same y. Verified in preview via geometry: 6/6 tiles nameH=23, textAlign=center, rarity offset identical (115px) incl. the 2-line Stardust. Prior: W628 — IAP GO-LIVE FLIP: auth.js IAP_ENABLED = true. Every gate verified first: 11 IAPs in ASC (Stardust .sovereign), RC key paste-verified (W627), .p8 In-App Purchase Key "Valid credentials" in RC, Paid Apps agreement ACTIVE thru Apr 2027 (bank + W-9 active), webhook chain both ends, first-100 grandfather deployed (cf1bad73). Verified in preview that the WEB channel stays inert with the flag on (no native RC plugin → iapAvailable() false → all tiles SOON, no Founder teaser — no priced dead surface on Netlify). Activates ONLY on iOS build 393+, which must be submitted WITH the 11 IAPs attached to the version (App Review then sees a working purchase flow;
first-IAP rule requires this anyway). At approval, everything lights at once: skins shop w/ localized prices, Founder pack, first-100 grandfather + congrats, unlimited Founder Ascent lives, Founder marks. Sandbox-test one skin + Founder on TestFlight before release. Prior: W627 — real RevenueCat production key wired in: auth.js REVENUECAT_PUBLIC_SDK_KEY = 'appl_phiMKPUJwIaixRkGliIxAfwOQVF' (the App Store app's PUBLIC API key, RC app id app2473dfedd7, created 2026-07-08;
public-by-design → safe in the repo;
the earlier 'app2473dfedd7' paste was the RC App ID, NOT a key — caught before wiring). Still fully DORMANT: IAP_ENABLED remains false, so nothing transacts. ⚠ TRANSCRIBED FROM A SCREENSHOT — owner must diff the committed string against RevenueCat's copy-button value character-for-character before the go-live build (wrong-byte-credential lesson: the W614 APPLE_TEAM_ID saga). Remaining go-live gates: (1) confirm the .p8 In-App Purchase Key (N7CY2QVR68 + Issuer ID) is uploaded in the RC app config, (2) Paid Apps agreement active, (3) submit the 11 IAPs with the next app version → review approval, (4) flip IAP_ENABLED in that build;
W626 first-100 backend still awaiting deploy-go. Prior: W626 — grandfathering switched to a CAPPED "First 100 Founders" promo + one-time congrats. BACKEND (iap-entitlements.ts): isFirstNFounder replaces the time-cutoff — founder iff the account's registration RANK (COUNT of users with earlier created_at, id tie-break) < FOUNDER_FIRST_N (default 100). Derived at read-time → idempotent, no backfill, can't double-grant;
ranks 0..99 = the first 100. WHY the switch: the old time-cutoff would have given the $49.99 Founder pack + unlimited Ascent lives FREE to EVERY 2026 joiner (cannibalizing sales);
first-100 caps it. ~30 accounts today so all current users are safely inside the first 100. CLIENT: _maybeShowFounderCongrats() (guard hb_founder_congrats_seen_v1, once-ever, set-before-show) fires when the server first confirms Founder, hooked into refreshSkinEntitlements (covers cold-launch 56588 + wardrobe-open + restore);
_showFounderCongratsModal = gold .fc-* card w/ the exact owner copy. Verified: fires once w/ correct copy, dismiss works, no refire when seen, non-founder never sees it. DORMANT: isFounder is display-gated on IAP_ENABLED (still false) so congrats no-ops until go-live. ⚠ HELD (NOT done): IAP_ENABLED stays FALSE — REVENUECAT_PUBLIC_SDK_KEY is still 'appl_REPLACE_ME' (no real key) + IAPs are unapproved drafts → flipping now = broken purchases + App Store 2.1 rejection;
needs real RC key + Paid Apps agreement + IAP submission/approval first. Backend NOT yet deployed (first-100 handler change gated on owner go). Prior: W625 — LIVE localized IAP prices in the shop (never hardcoded). auth.js Auth.getProductPrices(ids) → {productId:priceString} via RevenueCat rc.getProducts (same call as purchaseSkin, so a resolvable product is purchasable;
region/currency-correct). app.js _iapPrices (productId-keyed) + _refreshIapPrices() (gated on iapAvailable, inflight guard, repaints wardrobe + founder sheet on land);
fetched in openWardrobe + openFounder. Wardrobe SHOP badge: OWNED / <localized price, e.g. $4.99> / BUY-fallback-while-loading / SOON (dormant). SOON kept for the IAP-off state (a price there implies an un-honorable purchase). Founder price ALSO localized (_founderPriceStr → _iapPrices[founders_lifetime] || FOUNDER_OFFER.price) in the wardrobe teaser + _renderFounder price block + CTA — fixes an adversarial-review MEDIUM (hardcoded $49.99 next to localized skin prices for non-US users at go-live). Verified in preview: dormant=all SOON unchanged;
sim-live=mythic $4.99/legendary $2.99/owned OWNED;
€-locale sim shows €4,99/€2,99 skins + €49,99 Founder everywhere (no hardcoded $). Adversarial review (2 lenses) = ship, no correctness bugs. Dormant behind IAP_ENABLED. Prior: W624 — Stardust skin product-id fix. App Store Connect permanently reserved 'com.goallearner.awakened.skin.stardust' from an earlier draft (Apple never frees a used product id), so the live Stardust product carries a '.sovereign' suffix. Added _SKIN_PRODUCT_ID_OVERRIDES { 'avatar-skin-stardust.png' → 'com.goallearner.awakened.skin.stardust.sovereign' } so the client purchase call targets the real product;
backend PRODUCT_TO_SKIN maps BOTH ids to avatar-skin-stardust.png (old = dead alias). The other 9 skins + Founder are unchanged. ⚠ PENDING: owner must confirm the exact Stardust Product ID in ASC is character-for-character '...skin.stardust.sovereign' (else this breaks the same way);
backend NOT yet redeployed (gated on that confirmation) + RevenueCat must register the same id. Prior: W623 — Collection Log leaderboard LIVE: flipped LEADERBOARD_RELICS_BACKEND_ENABLED=true after the backend deploy (Worker version a8650a72, 2026-07-08, carrying relics_collected in metrics.ts). relics_collected now leaves _LB_CLIENT_ONLY_METRICS → the board fetches /v1/leaderboard/top?metric=relics_collected and lbSubmitAllMetrics submits the live log count. Verified: with the flag on, the signed-out preview routes to the sign-in gate (backend path) instead of the local-row dark mode;
endpoint reachable + auth-gated (401 pre-metric-validation for authless calls). First authed submit lands on the next device sync — watch `wrangler tail` if needed. Prior: W622 — Collection Log leaderboard (the race to complete the relic log). NEW metric 'relics_collected' = unique relics CURRENTLY held in the Collection Log;
_lbRelicsLogStats() mirrors _buildCollectionTree eligibility EXACTLY (rarity !== common + source_boss resolves in BOSSES/COOP_BOSSES → 59 eligible today, runtime-derived) and the discovered bit (W204 reverts it when the last copy sells, so the count can DECREASE — the backend's non-weekly plain-overwrite branch reflects that;
NOT monotonic by design). Backend: metrics.ts += relics_collected (cap 200, NOT weekly) + registry test — 326/326 pass;
NO migration (leaderboard_snapshots is metric-agnostic);
worker NOT yet deployed. Client: LEADERBOARD_RELICS_BACKEND_ENABLED=false (flip AFTER wrangler deploy, W274 pattern) → _LB_CLIENT_ONLY_METRICS;
deliberately NOT in _LB_SIM_METRICS (a race board gets NO fake bot competitors — while dark it renders the player's own local row at #1 instead of 'Be the first');
min-qualifying 1 (no 0-relic rows);
LB_CLIENT_CAPS 200;
LB_METRIC_META rows read '43 / 59' vs the live catalog total;
a COMPLETED log wears the floor_best-style gold crown ('Collection complete');
submit rides lbSubmitAllMetrics behind the flag;
local me-override + Global Rankings Hub row ('relics collected', gem glyph, Log: have/total). Verified in preview: hub 1/59 (seeded orphan card correctly NOT counted — eligibility proof), board title/blurb/own-row '1 / 59 RELICS', no sims, no console errors. Prior: W621 — pre-purchase surfacing of the W620 Founder benefit. (1) Settings "Become a Founder" banner: new .settings-founder-benefit gold mono micro-label under the tagline ("✦ UNLIMITED ASCENT ATTEMPTS · EXCLUSIVE LOOK · FOUNDER'S MARK";
wraps, no nowrap). (2) NEW Wardrobe teaser strip (.wd-founder-teaser, below the SHOP grid): ✦ glyph chip + "Become a Founder · $49.99 lifetime" (price from FOUNDER_OFFER.price) + "UNLIMITED ASCENT ATTEMPTS · EXCLUSIVE LOOK" micro-label (2 items — 3 ellipsized at 393px) + chevron;
gated _founderIapReady() && !_founderOwned() exactly like the W340/W343 prompts (dormant until IAP go-live, never shows to owned Founders);
tap = [data-wd-founder] branch in the wd-body delegation → closeWardrobe() then openFounder() (one sheet at a time, mirrors the Settings row). (3) Rank-up + perfect-day .fv-founder-cta buttons gain a .fv-founder-sub dark-on-gold sub-caption "UNLIMITED ASCENT ATTEMPTS · LIFETIME". Verified in preview: dormant = no teaser + settings row hidden;
live-IAP = teaser renders w/ price, fits at 393px, tap-through opens the Founder sheet showing the W620 benefit. Prior: W620 — Founder benefit (Path B, owner decision): Founders get UNLIMITED rated Ascent attempts;
free hunters keep the 2/day cap. FOUNDER_ASCENT_LIVES=Infinity (set a number, e.g. ASCENT_DAILY_LIVES+5, to switch to a boosted cap — everything routes through _ascentDailyLivesCap()). Consumers made founder-aware: ascentLivesLeft(), the W619 reconcile guard (a Founder mid-fight kill still RECORDS the loss past the free cap — verified dailyLosses 2→3, streak reset, marker cleared), finalize's livesLeft, the tower header (∞ + ✦ FOUNDER tag, gold), the hero CTA (✦ FOUNDER BENEFIT · UNLIMITED ASCENT ATTEMPTS note;
never locks), and the post-fight strip (UNLIMITED ATTEMPTS instead of a count). FOUNDER_OFFER copy: the "Never pay-to-win" benefit is REPLACED by "Unlimited Ascent attempts" (stated plainly). Ships DORMANT: isFounder() is display-gated behind IAP_ENABLED=false, so everyone sees the free experience until IAP go-live (all existing users grandfathered to Founder). NOTE owner-side: the App Store IAP review notes + review screenshot submitted for founders_lifetime still say "does NOT affect gameplay" — MUST be updated in App Store Connect before this rides a submission. Free-user regression verified (1/2 hearts, gate locks at 0). Prior: W619 — Ascent force-close life exploit closed. A rated fight only spent a life when its LOSS was persisted at the KO beat (arenaFinalizeBattle);
the whole interactive fight lived in-memory (_arSess) with nothing saved at fight-start, so an OS force-close mid-fight dropped the session and preserved the life → unlimited retries. Fix: _arCommitFight stamps hb_arena_pending {floor,date} at fight-START (rated only);
arenaFinalizeBattle clears it on ANY resolution (win/loss/forfeit);
_ascentReconcilePending() runs once at Arena init (setupArena) and converts a surviving marker into a rated loss (losses+1, streak reset, dailyLosses+1). SAME-DAY-only guard (marker carries the device-local date) so a cross-day abandon can't burn a fresh day's life after the daily roll-over. Client-only (localStorage hb_arena_v2, no backend). Verified live via the real setupArena path: same-day mid-fight kill burns exactly 1 life + breaks a 3-streak;
cross-day stale marker = no penalty, marker still cleared. Prior: W618 — Founder's Lifetime (one-time supporter pack) finished + kept DORMANT behind IAP_ENABLED=false. Net-new: Auth.purchaseFounders()/Auth.isFounder() in auth.js (mirror the RevenueCat skins flow;
isFounder display-gated on IAP_ENABLED);
Founder ✦ mark on the OWN hunter card (.pc-idrow) + OWN leaderboard row (dormant until live);
backend webhook now grants a reserved 'founder' entitlement via entitlementForProduct() (no schema change — rides skin_entitlements);
GET /entitlements returns {skins,founder} splitting the founder id out;
GRANDFATHERING = users.created_at < FOUNDER_GRANDFATHER_BEFORE_MS (env var;
default 2027-01-01 UTC → whole 2026 cohort auto-Founder). Cold-launch refresh gated by iapAvailable(). Nothing surfaced/charged: IAP_ENABLED=false + REVENUECAT_PUBLIC_SDK_KEY placeholder + backend undeployed. Backend 325 tests green (+1 founder-grant). [W617 — Drop History (Rewards) redesign ported from ClaudeDesign handoff 17: grouped date-shelf loot-log (66px art thumb, rarity ramp common→mega, dot-label states new/dup/cap, co-op/dungeon tag, gold-gradient serif title + "N Earned" pill). Namespaced .dh-* scoped to #drop-ledger-sheet.dh-sheet over the shared .vn-sheet shell;
reuses the hb_drop_ledger data + tap-to-reopen (openCardDetailModal) plumbing from W611/W612. [W616 — moved the "Rewards" (Drop History) entry OUT of the Relic Archive header (owner: it was in a "random place") INTO the archive filter/sort row, centered between the archetype segment (…MAGE) and the PWR sort (owner request). New .pdx-rewardsbtn in _pokedexFilterBarHtml with data-drop-history, wired via the delegated setupPokedex click handler (the bar re-renders). Gold-accented, margin-left:auto so the two auto-margins (it + PWR) split the gap → centered;
verified at 375px (gaps 7/7, no overflow). The header "N NEW" pill stays tappable → Drop History. [W615 — in-app drop-in banner on a FOREGROUND push. When a push arrives while the app is open, iOS suppresses the OS banner and fires pushNotificationReceived, which nudged _coopRefreshBadge()/_friendReqRefresh() — but those have 20s/90s poll throttles that SWALLOWED the nudge, so the in-app "Guild Request"/"Co-op Summons" banner didn't pop. Added a `force` flag threaded through _coopRefreshBadge(force)→_friendReqRefresh(force) that bypasses the throttle;
the pushNotificationReceived handler now calls both with force=true (a push is a definitive new-event signal). Existing pollers (boot/resume/dungeon) pass nothing → throttle unchanged. [W612 — FIX (adversarial review of W611 caught 2 real bugs). (1) HIGH: the Drop-History row tap opened openCardDetailModal WITHOUT closing the ledger first, so the relic detail (z-index 280) painted BEHIND the X-only vn-sheet (321) → the re-view was invisible + unreachable. Now closeDropLedger() before the detail opens. (2) MEDIUM: with the seen-key fix firing the boss-result modal on every co-op win (tiny common-less pools → mostly dupes), the modal claimed "RELIC ACQUIRED" while the toast said "Duplicate · cap reached". Threaded wasCapped/count into evt.drop;
the eyebrow now reads "DUPLICATE · CAP REACHED" / "DUPLICATE" honestly (fixes solo dupes too). [W611 — Rendell red flag: co-op relic DROP HISTORY + reveal fix. (1) New persistent, newest-first, local Drop Ledger (hb_drop_ledger, mirrors the souls ledger) written at the single rollBossDrop choke-point → records EVERY drop (co-op/solo/dupe/common/capped) so a reward is never lost even if the reveal pop-up misfired. (2) "Rewards / Drop History" bottom sheet (#drop-ledger-sheet, clones the souls-ledger sheet) — rarity-edged relic rows that tap to re-view (openCardDetailModal);
entry via an always-visible "⏱ Rewards" chip in the Relic Archive header (+ the "N NEW" pill made tappable) + a "View" CTA on the drop toast. (3) ROOT-CAUSE reveal fix: announceKillAndDrop's boss-result seen-key used the SOLO kill_count (getBossState), which never advances on co-op wins (they count in hb_coop_kills, and _awardCoopKill never calls setBossState) → repeat co-op wins early-returned before the reveal chained (relic stranded in reveal_queue = "notification fired but pop-up didn't"). Now keys off the co-op kill count (opts.coop, +1 pre-bump) so the ceremony fires every co-op win. [W606 — Armory v2 (ClaudeDesign handoff 13, the file the owner had open). Keeps the compact 4×2 grid but replaces .hunter-build-slot with the .gear-card recipe: slot + PWR overlaid on the art, item NAME in a caption strip BELOW the art (off it) so every card shares one baseline (the mockup's headline fix). Header restructured to v2 .head-main: ARMORY title left, close-x (#equipment-close, now .close-x) + Gear Power pill (.gp-pill) stacked top-right — owner explicitly chose this over W601's in-row pill. Refined .bonus-chip (fixed 56px) + .set-line (v2 grid + .active/.locked + .end). renderHunterBuild emits .gear-card (data-slot-index preserved → tap-to-equip router unchanged);
_SLOT_GLYPH_SVG no-art fallback;
mega variant added for mythic. Verified at 393px (4-col, both rows baseline-aligned) + 375px (2-col fallback). [W605 — FIX (adversarial review caught it): W604 wired Push.init() ONLY inside the visibilitychange RESUME handler, which never fires on a fresh page load — so a cold-launched session never uploaded its APNs token or attached the pushNotificationActionPerformed tap listener until the app was backgrounded+foregrounded once (the feature's PRIMARY case — app freshly opened — was dead, and a tapped push that cold-launched the closed app dropped its deep-link). Added a second, idempotent Push.init() to the REAL cold-launch setTimeout(1200) tail (next to Notif.rescheduleAll / _coopRefreshBadge). [W604 — Push notifications CLIENT (backend engine = W603). Added @capacitor/push-notifications;
a self-contained Push IIFE (window.Push) that, when native + signed-in + past onboarding, requests the (shared iOS) notification permission, registers with APNs, uploads the device token via Auth.registerPushToken → POST /v1/users/me/device-token, and deep-links a tapped push by payload type (friend_request/friend_accepted → Guild tab;
coop_invite/coop_joined → openCoopSheet(bossId));
foreground-received nudges the existing pollers to surface the in-app banner. Pollers (_friendReqRefresh/_coopRefreshBadge) STAY as the fallback. Push.init() wired into the cold-launch tail. Build scripts (codemagic.yaml + prep-local-build.sh) now write the aps-environment=production entitlement. REQUIRES owner Apple setup (APNs .p8 key + Push capability + regenerated provisioning profile) + the APNS_AUTH_KEY/APNS_KEY_ID worker secrets + a deploy before it lights up. [W602 — (owner Q&A) (1) NON-XP streak-milestone SOULS reward: 30d→300, 90d→900, 365d→3000 souls, once-EVER per (pack, milestone) via hb_streak_souls_v1_<pack>_<ms> (break+reclimb does NOT re-pay — exploit-proof, economy-safe;
amounts sit alongside boss-kill souls E:50…S:1600). Granted in awardCompoundEffect + the custom-routine path via shared _maybeGrantStreakMilestoneSouls;
surfaced as a violet "+N Souls · Milestone Reward" chip (#cp-souls) in the compound celebration popup. (2) FIRST-VOW POINTER FIX: onboarding's +25 XP made the old `totalPoints<=0` brandNew gate always false, so the pointer never showed — re-gated on hb_first_completion_bonus_v1 (unset until the first-ever manual check). [W601 below.] W601 — moved the Gear Power pill OUT of the Armory header and INTO the "Equipment Build" section row (owner preference): header is now just the ARMORY title + subtitle;
the row reads [Equipment Build] [258 Gear Power] on the left (grouped in .ash-left) and keeps "n / 8 Equipped" on the right. Un-scoped the .gear-power CSS (was .armory-head .gear-power) so it renders in its new home;
added .aw-section-head--gp (center-aligned). No JS change — #armory-gear-power / #armory-build-meta ids preserved. [W600 below.] W600 — Armory build grid reverted to the COMPACT 4×2 slot tiles (owner preference — the whole loadout reads at a glance, vs the taller W592 .aw-card cards). renderHunterBuild emits the previous .hunter-build-slot markup again and #hunter-build-grid is back to class="hunter-build-grid" (4-col ≥380px, 2-col below). KEPT the parts the owner liked from W592: the header Gear Power pill (#armory-gear-power, still fed by the accumulated gearPower/equippedCount) + the ARMORY title/subtitle/eyebrow fonts, and the bonus/set panels below. data-slot-index preserved (tap-to-equip unchanged). [W599 below.] W599 — FIX (adversarial review caught it): the W596/W598 guild-invite buttons never rendered in the shipping build — the add-row button lived only in _ensureSocialMarkup (a fallback that never runs because static index.html already provides #social-friends-body), and the empty-state button was in the SOCIAL_HUB_ENABLED===false branch (dead, flag is true). Moved the invite button into the LIVE static friends markup (index.html #social-panel add form) and into the LIVE guildhall-empty-friends empty state. Verified in the real DOM: one invite button, inside #social-panel, styled, sibling of #social-friends-body (survives friend-list re-renders). Wiring (_guildInviteShare + delegated [data-guild-invite]) + CSS were already correct. [W598 below.] W598 — Week-2 completion feedback + onboarding beats. (1) Light HAPTIC on every manual vow completion via a new general-purpose _hapticTick() that uses Capacitor Haptics (the REAL iOS path — navigator.vibrate is a no-op in WKWebView) with a vibrate fallback;
the plain check path previously had no haptic. (2) Guild-invite DISCOVERABILITY (onboarding beat 4): routed both invite buttons through the socialPanel delegated [data-guild-invite] handler and added an "Invite a hunter" CTA to the empty friends state so a brand-new user finds it. (3) DUNGEON TEASE (onboarding beat 3): _maybeShowDungeonTease() — a one-time crimson, tappable pointer on the Habits tab shown AFTER the first real vow is sealed (hb_first_completion_bonus_v1, not the onboarding +25 XP) and before the Dungeon's first-open coachmark;
tap → switchTab('quests'). Also corrected a stale playCheckSound comment (sfx_habit_check.m4a/.mp3 DO ship + precache;
the designed seal sound plays, synth is only fallback). [W597 below.] W597 — streak reward made VISIBLE (Week-2 retention): the escalating compound-XP curve (getCompoundXP) was a real reward loop but invisible — users saw "Day 12" with no sense of the next payoff or how close it is. Added _nextStreakMilestone(packId,streak) → the next day the daily compound payout steps up (7/14/30/90/180/365), its XP, and days-away;
surfaced as a gold "Next: Day 30 → +150 XP · 18 to go" sub-line on each compound-pack row in the All-Streaks sheet. Falls back to the static label for custom packs (no compound curve) and at max tier. [W596 below.] W596 — Guild invite viral loop (Week-2 retention): the only way to add someone was to type their EXACT existing alias, so there was no path to bring a real-world friend INTO the app. Added _guildInviteShare() — shares a pre-filled invite carrying the user's own hunter name (so the friend can add them back) + the App Store link, via the same navigator.share→clipboard fallback the boss-kill share cards use (with an AbortError guard so a cancelled share doesn't dump to clipboard). Surfaced as an "Invite a hunter" CTA under the Guild-tab add-friend row. [W595 below.] W595 — visual-consistency pass (styles.css only): tokenized exact-value spacing (gate-grid/bosses-list--cards gap 12px→--sp-3;
cd-stats-grid--six/custom-stat-grid/hi-stats-grid gap 8px→--sp-2;
settings-header padding 16/20/4→--sp-4/5/1 — zero pixel change), aligned the lone 6px 3-col stat grid (.osrs-panel→8px, matching the app's other stat grids), and brought the one light Cinzel heading (.guildhall-section-title 600→700) in line with the app-wide 700 serif titles. [W594 below.] W594 — skeleton loaders: migrated the plain-text / no-loader surfaces to the W590 skeleton system — co-op sheet ("Loading the hunt..." → hero skeleton), co-op hunter picker (→ rows skeleton via new _skelHtml string helper), Guild feed cold-open (rows skeleton, gated on truly-cold fetch so cached/local rows still show instantly), and the co-op dashboard list (cards skeleton;
it had NO loader before). PvP left as-is (already on-theme pvp-spinner). [W593 below.] W593 — Relic Archive: the screen was ALREADY the redesign (collapsible rarity sections, class-chip+power overlays, deltas-vs-equipped, meters, NEW tracking, mystery cards, marketplace);
added the one thing the ClaudeDesign refresh adds that the header lacked — a "· N% Complete" completion readout beside the discovered count. [W592 below.] W592 — Armory redesign (ClaudeDesign handoff 11): premium .aw-card gear tiles (art + slot·PWR banner + name + rarity), a header Gear Power pill summing equipped item power, a 2-col build grid, the "Sigil Grid" bonus panel (colored stat chips + gold TOTAL), and violet-keyed .set-panel(s) (crest + tagline + n/max progress track + per-tier locked/unlocked lines) in their own Set Bonuses section. renderHunterBuild emits .aw-card buttons (data-slot-index preserved → tap-to-equip router unchanged);
.aw-card base rule reset for <button> on iOS WKWebView. [W591 below.] W591 — marketing version 2.4.1 -> 2.4.2 (2.4.1 approved by App Store Connect;
next train). Bumps the APP_VERSION single-source-of-truth that prep-local-build.sh forces into iOS MARKETING_VERSION. [W590 below.] W590 — reusable skeleton/loading system: on-theme "etched runes" shimmer (violet→gold sweep + breathe, tokens only, reduced-motion aware) via showLoading(target,kind,count)/hideLoading(target) with composed templates (rows/grid/cards/duo/hero/line);
exposed on window. Wired into the friends list (was plain "Loading friends…"). Verified: 18 skels render + animate. [W589 below.] W589 — boss-name truncation, fully fixed. W588's 2-line clamp still cut long names (THE ASCENDANT COLOSSUS, THE FORGE OF TEN THOUSAND DAYS, EREBUS THE SHADOW SOVEREIGN wrapped to 3 lines and ellipsized). Now: .bcard-name is a measurable block with length-bucket CSS sizes (--long 18-21ch, --xlong 22ch+) as the start, and _fitBossNames() shrinks any name still over two lines to a floor of 7.5px, re-running on document.fonts.ready. Verified with real Cinzel metrics: all boss names land at exactly 2 lines (short stay 12.2px, longest Erebus 8.9px), none clipped. [W588 below.] W588 — polish sweep #1 (text truncation): dungeon boss-card names (.bcard-name) now wrap to 2 lines instead of hard single-line ellipsis (THE DREAM TYRANT / THE FURNACE KNIGHT / THE SUNDERED CHOIR were clipping);
.bcard-header min-height 30->42px for the 2nd line. Reuses the relic archive's proven 2-line clamp. Verified: longest boss-name word (INSOMNIAC, 82px) fits the ~113px box, no horizontal overflow. [W587 below.] W587 — Armory/relic readability: (1) equipped-item power now shows beside the slot type on the Armory build cards ("CAPE 32 PWR", via _relicProfile matching the Items-tab number);
(2) the equip PICKER (openBuildPicker) sorts highest-power first so the best item to equip is on top;
(3) the Items-tab relic grid (renderPokedex) defaults to power-descending (within each rarity section), toggle still flips to the discovery-log chronology. [W586 below.] W586 — NOTIFICATION SYSTEM REVAMP: all 5 local-notif moments rewritten in a plain, competitive, emotional voice (Duolingo/Strava, not lore) as rotating pools (182 variants) that personalize with name/streak/today's-undone-vow AND live STEP-RANK + guild rivalry from the leaderboard cache (_notifStepCtx via lbCacheRead + _lbMaybeSimulate so the rank matches in-app World Rank). Daily rotation (no repeat 2 days running), token-fill only when fillable (cache-miss safe). NEW comeback/lapsed notifs (d2/d4/d7, ids 99996-99994) re-armed on every app-open reschedule. Builders now return {title,body};
Settings voice-preview + Notif export updated. [W585 backend security below.] W584 — jump-program guide row: The First Awakened lives under the vows for jump users (#fa-program-guide, gold portrait row);
tapping him REPLAYS the program walkthrough (same FA_JUMP_WELCOME_BEATS via _faRunCoachmark, context jump_welcome_replay, CTA BACK TO TRAINING, no storageKey -> replayable forever). Only the closing line swaps (the signup-time "first cycle begins Monday" -> evergreen). Hidden for non-jump users;
toggled in renderHabits before the fingerprint bail. [W583 below.] W583 — kill-club stamp upgrades every 100 boss kills: the crimson leaderboard chip's label is now the highest century crossed (100/200/300…, e.g. 279 kills -> 200) instead of a hardcoded 100;
chip width flexes for 3-4 digit tiers. Client-only — the raw bosses_slain count already rides the /v1/leaderboard/top payload. [W582 below.] W582 — FRIEND REQUEST drop-in banner: incoming friend requests now surface as a top summons-style banner anywhere in the app (gold .csb--friend skin on the csb component;
Accept / Accept-all-N straight from the banner with the same Guild-Board bookkeeping as the Guild-tab Accept;
tap-through -> Guild tab;
dismissed/acted ids persisted). Detection piggybacks _coopRefreshBadge's call sites with its own 90s throttle on /v1/friends;
the co-op summons outranks it when both are pending. [W581 below.] W581 — jump SESSION ORDER: the daily list renders in template-index order, so the jump block is re-indexed so every session lists its warmup FIRST then exercises in Richie's program order (D1: warmup,sprints,depth,weighted,max;
D8: warmup,sprints,broad,band,max;
D3/10: CNS warmup,max,box squats,calves;
D5/12 unchanged). Max Effort Jumps moved after BOTH warmups (it was index 56 — rendering above Wednesday's CNS warmup, the reported bug). Safe re-index: jump block is the DEFAULT_HABITS tail (49-66), pack seeds the whole set, all other wiring is name-keyed;
no prod jump users exist yet. [W580 below.] W580 — jump cycle CALENDAR-LOCKED to the Mon/Wed/Fri grid (day = weekday + A/B week parity, Week A anchors to first Monday on/after signup;
mid-week signups join at that day's session, weekend signups idle to Monday) + FA_JUMP_WELCOME_BEATS: jump users get a plain-tone First Awakened program walkthrough (schedule, exercise usage, dailies, cycle) instead of the default first-vow welcome. [W579/W578/W577 below.] W579 — cycle-aware history + streaks. W578 — jump habit icons. W577 — co-op boss cards open the RECRUIT/invite sheet directly (fixes orphaned invite-sending: W394 routed cards into the Pacts dashboard, then W572's Accept-All replaced the dashboard's Recruit button, leaving NO way to send an invite;
The Pacts strip remains the dashboard entry for progress + accepts). Also first bump since W573 — ships W574/W575/W576 (Vertical Jump Program: onboarding goal flag + library partition + true 14-day cycle scheduler + Richie's real program data, indices 49-66). [W573 below.] W573 — bump marketing version 2.4.0 -> 2.4.1 for the next App Store train (build 378). [W572 below.] W572 — THE PACTS redesign (ClaudeDesign handoff 10): co-op hunts grouped (Pending Invites / Active Hunts) + sorted by time remaining (soonest first);
a prominent LIVE urgency-colored time pill (crit <2h red-pulse / warn <6h gold / teal) with a 20s ticker;
full progress (no clipping);
inline invite Accept Pact / Decline;
footer Accept-All (>=2 invites) REPLACES the Recruit button (recruiting still lives on the dungeon co-op-boss sheet). [W571 below.] // W571 — Collection Log: owned relic tiles now show their real art_path image (was just the slot-glyph silhouette). Reveal-on-load via _clLoadArt (img fades in on load, falls back to the slot glyph on error — matches the app's robust loader, no inline onerror). Mystery/undiscovered tiles keep the silhouette. [W570 below.] // W570 — COLLECTION LOG (Items tab Archive-Collection lens toggle). Dungeons by rank (co-op bosses merged + silently tagged) -> bosses -> rare/ultra/mythic relic tiles with per-relic count + mystery states + completion meters + locked/done gate flourishes. Built live from CARDS+BOSSES+COOP_BOSSES+hb_inventory via _buildCollectionTree (nothing hardcoded). Ported from ClaudeDesign. Verified: render smoke test renders all 6 dungeons, 21 bosses, 59 non-common relics (matches the placement plan), 2 locked gates at B-rank, owned vs mystery, completion. [W569 below.] // W569 — finished greaves->boots: all 6 leg-armor items now slot:'boots' (retired the 'legs' slot;
W563 had only remapped the typed slot). Fixes equip screen + Collection Log glyph;
safe _migrateLegsToBoots() consolidates old eq.legs into eq.boots when free. [W568 below.] // W568 — Aetherspire (mage staff) + Wraithwind (ranger bow) moved from shop-only to A-rank DROPS (Sleepless Ascent + Tideless Marcher ultra pools). They were the only buyable-but-undroppable ultras;
now droppable AND marketplace-tradeable like every other ultra (no access lost). Side effect: 0 shop-only relics left, so the collection log is 100% dungeon drops. [W567 below.] // W567 — sustain tune (Rendell/owner): Siphon + Ward Strike (twins: 0.7 dmg + 12% heal) cooldown 1 -> 2 in BOTH engines, so the heal-strike can't be spammed every single turn. Sim (sustain sentinel vs aggro, type-eff isolated): 70.4% -> 68.2% sustain win — a deliberately modest -2.2pt trim that kills the spam without gutting sustain (owner+Rendell: don't make heal useless). Mage tankiness HELD (intentional INT->defense bulk). parity 4/0. [W566 below.] W566 — type-effectiveness LABEL honesty (Rendell feedback): the three "super effective" edges differ a lot by design (+20%/+18%/+8%, tuned for equal win-rate across archetypes of differing durability), so a flat SUPER EFFECTIVE over-promised on the +8% sentinel>aggressor edge ("sometimes does nothing"). A matchup sim confirmed re-tuning the multipliers is kit-sensitive + risky pre-launch, so the fix is label-only: graduated by magnitude (win >= 1.13 => SUPER EFFECTIVE, below => FAVORED;
mirror NOT VERY EFFECTIVE/RESISTED) in BOTH engines + the Tale-of-the-Tape verdict. ZERO combat-math change, parity 4/0. [W564 below.] W564 — THE PACTS entry banner (ClaudeDesign handoff 8): a discoverable co-op hub entry on the Dungeon tab, below the rank gates. Teal co-op identity + ally-face cluster;
3 data-driven states (pending invites w/ count badge > live hunts w/ blink dot + soonest clock > empty/summon-ring), all from the same coopBossList the badge poller already fetches (_pactsEntryRender hooked into _coopRefreshBadge + renderQuestsPanel). Opens the existing Pacts hub (_coopOpenDashboard). Static #pacts-entry in index.html + self-contained .pacts-entry CSS. [W563 below.] // W563 — GREAVES → BOOTS: leg armor (greaves/legplates, data slot 'legs') was folded into the ARMOR/plate equip slot via LEGACY_TO_TYPED_SLOT (legs:'plate'). Re-pointed legs→'boots' so greaves categorize under BOOTS, not ARMOR — they now compete with boots, not body armor. normalizeHunterBuild gained a slot-reconciliation migration: a now-mismatched equipped card relocates to its correct slot (or unequips, never orphans). Sets: co-op (Choir/Wardens) unaffected (no legs piece);
Sovereign Regalia's 4 pieces are now wearable in distinct slots but it intentionally stays 3pc (revisit if a 4pc is wanted);
Wakeful Vigil 4pc still reachable. Booted clean, no errors. [W562 build-script-only, no web bump.] [W561 below.] // W561 —APP_VERSION 2.3.3 -> 2.4.0 (2.3.3 approved + released by Apple;
new marketing version for the next App Store train carrying the F100 finale, new icon+logo, speedrun clock, co-op feed, rank-color unification, burn nerf). prep-local-build.sh feeds APP_VERSION to agvtool. [W560 below.] // W560 — BURN NERF: the burn DoT was 16% of max HP base (8-15%/turn after the 0.5-1.0x power scale + 15% DoT cap = ~30-50/turn on a high-HP hunter, in both Ascent + PvP). Cut the base mag 0.16 -> 0.10 on all 3 burn moves (Searing Cut, Immolate, Flame Arrow) in app.js AND backend combat-core.js (byte-identical, parity verified). Now ~5-10%/turn (~16-33). Bleed (0.12) + the DoT cap (0.15) unchanged. Verified: parity 4/0, smoke 12/0, draw 9/0, attune 6/0;
F100 still beatable both weapons (Nightfall 72%, Duskforge 63% BiS, 90%+ invested). Backend re-deployed. [W559 below.] // W559 — rank-color consistency: unified ALL hunter-rank (E/D/C/B/A/S/S+) coloring on the canonical [data-rank]/_PC_RANK_COLORS palette (E purple, D cyan, C emerald, B amber, A red, S fuchsia, S+ gold). Audit found 4 divergent palettes;
canonicalized 11 surfaces — Hunter Profile disc + identity text, boss-card pill, Kill Log value + group accent, souls-ledger chip, 100K-Club badge, rank-up screen fallbacks, plus JS maps RANK_EFFECTS + HR_RANK_PALETTE. data-rank surfaces now consume var(--rank-color) so they can't drift. Non-rank colors (class/stat/drop-mercy/streak/class-gem) intentionally untouched. [W558 below.] // W558 — fix: home "BOSSES·SLAIN" tile (and other tallies) were inconsistent with the Kill Log "TOTAL KILLS" because the co-op backfill (hb_coop_kills ← backend completed co-op wins) ran ONLY on Kill-Log open and repainted ONLY the Kill Log;
the home card rendered on load before that, stuck at the solo-only count. Now _coopBackfillKills runs once per session (kicked from updateHeaderMetrics on load), guards against re-entry, and on success repaints the home card + guild summary + Kill Log. [W557 below.] // W557 — co-op dungeon clears now show in the Hunter activity feed as "You cleared <dungeon> · +N souls" (combat icon) instead of a bare "Souls earned" line: added a 'coop_' branch to _classifySoulsEvent (earnSouls hint 'coop_<bossId>' → type coop_kill, label "Cleared <name>" via COOP_BOSSES), a dedicated _hunterFeedCoopKillRow in the merge pass, and a one-time _migrateCoopLedgerLabels that relabels pre-W557 rows (their detail still carries 'coop_<id>'). Verified live: new win + migrated legacy row both render correctly. [W556 below.] // W556 — HOME-SCREEN APP ICON: regenerated the full iOS AppIcon set + PWA icon-192/512 from the de-squared gold-"A" art (app-icon-source.png) via the canonical scripts/generate-app-icons.ps1 — opaque 24-bit RGB (no alpha;
Apple rejects transparent icons). Auto-applied to the build by prep-local-build step 8b (patch-ios-app-icon.sh: resources/ → ios xcassets) and hash-verified by step 11. The square-framed icon is gone on the home screen too, matching the new splash logo. [W555 below.] // W555 — app logo: the gold-"A" AWKND mark (transparent assets/awknd-logo.png, cut from the de-squared + de-starred Nano-Banana art via goldness key) replaces the splash triangle Spark emblem AND the Cinzel "AWAKENED" wordmark;
downscaled to 800² (~99KB) so it stays light on the critical splash paint. [W554 below.] // W554 — "Accept all" on the co-op summons banner. When an ally has called you to ≥2 hunts at once, the top banner's CTA becomes "Accept all N" (bulk-join in one tap) instead of answering each;
a single invite keeps "Answer" (opens the detail). _coopBannerAcceptAll(invites) mirrors the W528 dashboard bulk-join (same per-join API + 320ms spacing + summary toast), acts on the authoritative invite list the badge poller already fetched (passed via _coopShowInviteBanner(inst, allInvites)), and shares the _coopDashAccepting guard so the banner + dashboard accept-all can't run at once. Verified the loop joins all pending invites (3/3) and filters out non-pending. [W553 below.] // W553 — "TIME TO SUMMIT" any% speedrun clock on the F100 finale. Anchor = the player's first day in Awakened → the Floor-100 clear. _journeyStartMs() sets the start ONCE (precise "now" for new players, captured at first boot in load();
midnight of the earliest hb_completions day for existing players). End = the recorded Hall finish (hb_hall_finish.at) with a once-captured local stamp (hb_summit_finished_at) as an offline-stable fallback. Both keys added to SNAPSHOT_KEYS so a reinstall can't reset/drift the clock. Elapsed renders as "Xd Yh Zm" (_fmtSpeedrun) on the summit card (beat 5) AND the static/reduced-motion summit. Verified on the card (87d 4h 12m from a seeded clock) + boot/co-op e2e green. [W552 below.] // W552 — a11y pass on the W548 Ascension cinematic: the overlay is now role="dialog" with a descriptive aria-label ("The Ascension — Floor 100 complete. The Second Awakened."), and all 14 purely-decorative layers (sky/tower/figure/particles/letterbox/burst/scrim) are aria-hidden so screen readers announce only the meaningful beat text + the Share / Enter-the-Hall actions (both verified reachable by AT). Motion-sensitive / most AT users still get the fully-accessible static summit via the reduced-motion guard. (Rest of the app already well-covered: 80 aria-labels, 57 reduced-motion rules.) [W551 below — audit fixes.] // W551 — fix 3 bugs an adversarial audit found in the launch-critical paths. (1) [HIGH] BOTH endgame cinematics (.asc-cine W548 + .fa-cine W539 Unbind) mounted on <body> at z-index 9000, BELOW the visible arena overlay (.ar-overlay z 12300) — so on the REAL fight/victory path they rendered behind an 82%-opaque blur and (for .asc-cine) trapped every tap;
raised both to z 99000 (the established above-arena cinematic tier, matching .ao-opening). Proven with elementFromPoint: cinematic is now the top hit target + Enter-the-Hall reachable. (2) [MED] co-op kill double-count: _coopBackfillKills counted completed+success instances that the live _awardCoopKill had not yet bumped, then the live +1 ran → durable +1 (hb_coop_kills is snapshotted);
backfill now counts ONLY already-awarded instances (disjoint from the live bump). (3) [LOW] _arPlayAscension teardown made idempotent (el.__bye guard) + .asc-bye gets pointer-events:none so a double-tap of "Enter the Hall" can't fire onHall/_arRenderHall twice. A 4th finding (F100 turn-cap "false summit") was REFUTED by 45k simulated fights (the damage tally is decisively against a phase-2 staller). Added tests/e2e/coop-kill-ledger.spec.ts. [W550 below — asset only.] // W549 — PUBLIC-BUILD KILL SWITCH: gate the W542 hidden dev test panel behind a single DEV_BUILD const (true for TestFlight, flip to false for the public App Store build → the long-press becomes inert and the panel is unreachable). Verified _devOpenPanel is reachable ONLY via the now-gated _devWireLongPress. Also audited launch-safety: the W533 15k beta-souls grant is fully reverted and the older W198 Founder's Gift was removed at W200 — no large-soul grant leaks to public;
only the intended 35-soul first-install grant + daily bonus remain. [W548 below.] // W548 — THE ASCENSION: Floor-100 victory finale (ClaudeDesign handoff (7)). The gold mirror of the Unbound cinematic — a 5-beat, reader-paced (tap-to-continue) cinematic that REPLACES the static summit: Release (crimson→gold) · The Passing (struck taunt + blessing) · The Title (THE SECOND AWAKENED gold slam) · The Hall (your REAL eternal ordinal + the real "those who came before" roll) · Hand-off (shareable summit card + finale-theme swell). _arPlayAscension() overlays the static summit (which remains the reduced-motion/fallback resting screen);
binds the live Hall ordinal/names/date/avatar (canon placeholders pre-backend);
"Enter the Hall ▸" tears down → _arRenderHall. Dev panel gains a "👑 The Ascension" trigger. Verified beat-gating + data-binding + tap-advance + teardown in preview;
god-rays kept static (no infinite rotation) for mobile-webview perf. [W547 below.] // W547 — close the review-prompt flush gap on the boss-result modal: previously a WIN dismissed via "View Relic" or "View Mercy" (suppressDrain:true closes) skipped _reviewFlush, so a hunter who tapped straight into their loot on a first boss/co-op win never got the one-shot review pre-prompt. Moved the flush out of the drain guard + added an opt-in flushReview flag passed ONLY by the two deliberate win-engagement closes (View Relic 15646, View Mercy 15664);
Hunt-Again re-engage, the masked-relic reveal cinematic, and incidental tab-switch still do NOT flush (leave it armed for a later trigger). Still WIN-only, one-shot, READY-gated downstream. Verified boolean truth table across all 8 close/outcome combos. [W546 below.] // W546 — co-op dungeon kills now folded into EVERY user-facing "bosses slain" tally, not just the Kill Log rows: one shared _coopKillTotal() (sum of hb_coop_kills counts) added to the home BOSSES SLAIN tile, the top-bosses-slain-total stat, the guild-roster self stat, _publicAchievementsSummary, and the Hunter Record card — so the numbers match the Kill Log ledger (which already merged solo+co-op in W545). [W545 below.] // W545 — CO-OP dungeons now appear in the Kill Log (Bosses Slain sheet). Co-op wins had no per-boss count;
added durable hb_coop_kills (SNAPSHOT_KEYS), incremented in _awardCoopKill, folded into the same ranked log via _guildCoopSlainRows (tagged CO-OP), with a best-effort backend backfill (_coopBackfillKills counts completed+success instances per boss → past wins appear without re-winning). Dev co-op button now records a kill too. [W544 below.] // W544 — the F100 SUMMIT finale (_arRenderSummit) now plays the bespoke first_awakened_win (Suno) instead of the generic victory_sting (fallback if absent). Quick win ahead of a ClaudeDesign dramatic-finale pass. [W543 below.] // W543 — two on-device fixes: (1) first-awakened-unbound.png had a baked BLACK background (Nano Banana output) → flood-filled the bg to transparent (corner-seeded, kept the figure + glow) so the Unbound portrait floats cleanly in the cinematic AND the battle;
(2) the dev panel's "Boss-defeated celebration" built too thin an evt → now calls the REAL announceKillAndDrop(getBoss('the_steel_wolf'), 50, null) so the actual modal + fanfare show. [W542 below.] W542 — ⚠️ DEV TEST PANEL (TestFlight testing aid). Hidden behind a long-press (~0.85s) on the Settings "Version" line;
opens a panel to trigger every new set-piece on-device without grinding: the F100 unbind cinematic, all 3 review pre-prompt variants, the perfect-day + boss-defeated celebrations, and a review-flag reset to re-test the one-shot. Fenced block _devOpenPanel/_devWireLongPress (+ the _devWireLongPress() call in setupSettings) — DELETE before the public App Store build. Low-risk to leave (hidden gesture) but not for public. [W541 below.] W541 — compliant ONE-SHOT App Store review pre-prompt (Guideline 1.1.6/5.6, adversarially reviewed = compliant). A branded modal (3 variants: boss/co-op/perfect-day) at a peak moment whose ONLY CTA fires Apple's native review sheet via @capacitor-community/in-app-review (Capacitor.Plugins.InAppReview.requestReview());
"Not now" closes only. No sentiment fork, no reward, no custom stars;
all gating on our own hb_review_* flags (CloudSync-durable), never on review outcome (no callback exists). Fires once on the first qualifying win (co-op>boss>perfect-day;
perfect-day READY-gated so a day-one user can't burn the shot), AFTER the celebration closes — and ONLY after a WIN dismissal (outcome-aware, never a HUNT FAILED close). Settings → "Rate Awakened" = user-initiated write-review deep link. ⚠️ MacBook: `npx cap sync ios` to install the pod before building. [W540 below.] W540 — slowed the Unbound cinematic 2.5s→6s (owner: "way too fast, can't read"). All beats scale with --dur, so only the base DUR changed;
re-anchored the JS figure-swap (DUR*0.38) + overlay teardown (DUR*0.18) to fractions of it. (FF halves it to 3s.) [W539 below.] // W539 — UNBOUND phase-transition CINEMATIC (ClaudeDesign port). The F100 'phase' handler now plays _faPlayUnbindCinematic: a full-screen 2.5s, 5-beat overlay (fake-out → real portrait CRACKS + shatter flash + shards → fractured spark sigil + shockwave rings + "UNBOUND" title slam → taunt → fresh crimson bar → resume). Faithful to cinematic-*.jsx (beats/colors/eases) but rebuilt with the REAL portraits (idle→unbound) per owner, not the geometric mask/silhouette. CSS-@keyframes choreography (.fa-cine*, delays = fractions of --dur);
JS schedules the figure-swap + onReveal (battle name/portrait/part-2 music) at the shatter, onDone (phase-2 bars + resume) at hand-off. Respects _pkbFF (half-time) + prefers-reduced-motion. ⚠️ v1 timing/positions are best-guess (can't preview F100) — tune on device. [W538 below.] // W538 — First Awakened UNBOUND portrait. Real menacing low-poly art (assets/coach/first-awakened-unbound.png, generated from the actual sage via Nano Banana) now swaps in on the phase-2 transition: _arFoeArt returns it when foe._faPhase===2, and the live 'phase' handler hot-swaps the .spr img src. Softened the .fa-unbound CSS (dropped the saturate/hue-rotate that was faking menace on the navy idle art — the new art is already crimson;
kept the red glow + flash). [W537 below.] // W537 — First Awakened BESPOKE MUSIC bundled (Suno): assets/audio/first_awakened_loop.mp3 (battle theme, part 1) + first_awakened_win.mp3 (finale) are now live at F100. No first_awakened_phase2 track yet → phase 2 now continues the FA THEME (first_awakened_loop) instead of dropping to the generic boss_loop (was boss_loop). Auto-swaps to a distinct part-2 track if/when first_awakened_phase2.mp3 is added. [W536 below.] W536 — FIXED W535's phase-2 KIT bug (adversarial review): _arenaFoeKit floor-gates F100 to the ranged summit kit, so phase 2 fought with the phase-1 EVASIVE moveset instead of the aggressor blitz (the style-switch was nullified). Now built straight from ARCH_MOVES[aggressor] (cleave/sunder/slash/focus). Re-tuned: the aggressor kit dies faster (low DEF→HP) so it's inherently EASIER than the evasive kit → _FA_P2_POWER 0.82→1.02 to restore the approved wall (mid-BiS ~49% / top ~52% / sub-BiS ~20%, top>mid so strength still helps;
stayed under ~1.1 where the glass-cannon race inverts the skill gradient). selfTest 37/37. [W535 below — its "0.82 / 48-55" numbers are SUPERSEDED by this.] W535 — THE FIRST AWAKENED IS NOW A TWO-PHASE BOSS (Kalphite-Queen "you only met the mask" twist). Phase 1 = the balanced summit (unchanged);
on its defeat it does NOT die — _arMaybeAdvancePhase() unbinds it into "The First Awakened, Unbound" (AGGRESSOR archetype, _FA_P2_POWER 0.82 × floor power), heals the player +50%, refills the foe HP, and continues the fight. The win only fires when phase 2 falls. Sim-tuned so only a near-maxed hunter clears BOTH (mid-BiS Duskforge ~48% / top Nightfall ~55% / sub-BiS ~20%). Engine lives in the SHARED win-check so simAscent plays both phases → selfTest 37/37, fully guarded (every non-F100 fight byte-identical). UI: _pkbBeat 'phase' beat = fake-out lines → name/HP/portrait swap (crimson "fa-unbound" menace + white unbind flash) → part-2 music. Music slots first_awakened_loop/_phase2/_win added to _AUD.slots, gated to F100, GRACEFUL FALLBACK to boss_loop/victory_sting until the Suno files are bundled. NOTE: this is a NEW feature on top of the clean public 2.3.3 (build 370) — it ships in a build AFTER it. [W534 below.] W534 — REMOVED the W533 TestFlight beta souls grant (testers claimed their 15k). This is the CLEAN build for the public 2.3.3 App Store release: no on-device souls grant anywhere. The leftover localStorage flag hb_beta_souls_15k_v1 on tester devices is harmless/unused. SUBMIT THIS (build 370+) to the public App Store — NOT TestFlight build 369, which still contains the grant. [W533 below, now REVERTED.] W533 — ⚠️ TESTFLIGHT-ONLY BETA SOULS GRANT. One-time 15,000-soul thank-you to TestFlight testers: fires once per device on init() right after loadSouls(), gated by localStorage flag hb_beta_souls_15k_v1 (no farming on reopen), sticky toast confirms. MUST NOT ship to the public App Store — the grant block (clearly fenced in init) has to be DELETED and a clean build cut for public release, or every public user claims 15k free souls (economy inflation + souls-IAP undercut, unrecoverable). Revert = remove the fenced block + this note. NO other logic change. [W532 below.] W532 — APP_VERSION 2.3.2→2.3.3. Apple closed the 2.3.2 pre-release train after build 367;
the 2.3.2 build-368 upload was rejected (90186 "train closed" + 90062 "CFBundleShortVersionString must be > previously approved 2.3.2"). prep-local-build.sh reads APP_VERSION from app.js as the single source of truth → agvtool new-marketing-version, so this bump alone fixes the upload. NO code-logic change;
2.3.3 carries the already-built W527–W531. [W531 below.] W531 — COMBAT BAR "Forged Plate" (ClaudeDesign handoff). Reskinned the per-turn move buttons (shared _pkbRefreshMoves card() → Ascent + ranked PvP + Echo all at once) from plain vertical web buttons into forged-metal cards: glowing 40px accent icon tile + Cinzel name + Cormorant-italic flavor + a stat chip. Per-move ACCENT derives from the move's effect via _arMoveType(mv)/_AR_MOVE_ACCENT (no per-move table): self-buff/heal/cleanse (heal/defUp/atkUp/dodge/guard/guardCleanse/bulwark) → DEFENSE teal·SUPPORT;
foe affliction (defDown/atkDown/burn/bleed/stun) → DEBUFF violet·FOE;
else → ATTACK gold·PWR<power×100>. Three states: default · selected (.sel added on tap in the click handler → accent glow + sheen + "Selected" badge, PERSISTS through the PvP wait-lock, cleared on next _pkbRefreshMoves) · cooldown (.cd → grayscale + conic .cdnum ring + "N TURN(S)"). Trimmed Bulwark's combat desc to fit the taller card. CSS rewrite at styles.css .ar-bmove* (old .top/.g/.ds/.tg/.cdn removed, no dangling refs). Verified: selfTest 37/37, node --check, adversarial fidelity review = SHIP. [W530 below.] W530 — MELEE "BULWARK" combined stance (owner: "guard buff for melee", full guard+focus combine). New ARENA_MOVE_LIB.bulwark (gl shield, power 0, cd 5, fx {t:'bulwark', mag 0.40, dur 3}) = Guard (-40% taken) + Focus (+40% atk) + cleanse/cauterize in ONE move, 3 turns. _arApplyFx case 'bulwark' CLEANSES FIRST (the guardCleanse filter strips mag<0 mods → would eat the def buff) then pushes def 'guard' -0.40 / atk 'focus' +0.40 / dotImmune 'cauterize'. Slotted into the 3 PURE-AGGRESSOR weapons only (titan_oathblade, kilnforged_warblade, duskforge_greatblade — replacing brace/temper);
NOT nightfall (universal → would leak to mage/ranger) NOR sentinel/trickster weapons. AI taught to use it: _aiEffectValue('bulwark') = guard+cleanse+def+atk summed;
_arenaPickMove + _aiDiscipline route bulwark through SETUP (primary), EMERGENCY-guard, and CLEANSE branches;
_AI_DEF_FX.bulwark=1 for counter-timing. T16 selfTest updated (kiln-AI now reaches Bulwark, not Temper). Mirrored in backend/src/pvp/combat-core.js (move + fx handler + 3 kits;
backend has NO AI so no picker mirror) — deploy DEFERRED until the client build ships (PvP desync otherwise). Sim: selfTest 37/37;
max duskforge F100 59%→90% (now ≈ nightfall 87% — fixes the BiS weapon gap);
curve still STEEP — A-rank (~279pw) F100 = 1.7%, so underleveled players still can't cheese the summit. Tunable: cd (raise to 6-7 if melee dominates PvP) + the 0.40 mags. [W529 below.] W529 — RANGER (trickster) PASSIVE EVASION. _arRangerEvade(defC, hpFrac, power) added a trickster-gated dodge term to the combat hit-resolution: scales with the INCOMING hit's power (Math.max(0,power-1.0)*0.10 — heavy committed swings are easier to read/roll) AND with MISSING HP ((1-hpFrac)*0.22 — a cornered hunter fights wild), bounded at +0.28 and folded under the hard _DODGE_CAP (0.50) so even low-HP+evade-up+big-hit can't beat a coin-flip. Archetype-gated via _arenaArchOf → SYMMETRIC (your ranger AND ranger foes), non-tricksters get 0. Mirrored EXACTLY in backend/src/pvp/combat-core.js for PvP parity (deploy DEFERRED until the matching client build ships, else PvP desyncs). Sim: Arena selfTest 37/37, Ascent still beatable (mid-ladder 100%;
summit ~86%/70% Nightfall/Duskforge — trickster summit floors got a bit harder as the symmetric cost, back toward intended difficulty). Tunable: the 0.10/0.22/0.28 constants. Guard stays melee-side + mage untouched per owner (no change needed — guard was never a mage move). [W528 below.] W528 — CO-OP "ACCEPT ALL": when ≥2 co-op hunts are pending where you're the recruited partner ("X summoned you"), a teal "Accept all N invites" bar appears atop the Pacts dashboard (#coop-dash-list). One tap joins them ALL sequentially (320ms gap) via Auth.coopBossJoin — the same path as a single "Answer ›" — so the shared RL_COOP_WRITE bucket (20/min) isn't burst-tripped. Partial failures summarized via showHabitToast ("Accepted N of M" / rate-limited), never thrown;
_coopDashAccepting guards re-entry;
_coopDashLoad refreshes after (accepted invites become active hunts, drop out of the invite set). Gated ≥2 (a single invite just uses the card's "Answer ›"). Files: app.js (_coopRenderDashboard bar + the list-delegation wire + _coopDashAcceptAll) + styles.css (.cph-accept-all). [W527 below.] W527 — ITEM-SLOT AUDIT + fix. Owner flagged The Hollow Crown shown as a WEAPON though its name/art are a literal crown ("I don't want that crown as a weapon"). Multi-agent audit of ALL 95 items (3 passes + per-flag adversarial verification) found it the ONLY genuine misplacement;
other flags were deliberate caster-focus weapons (Vessel of Refusal / Twofold Gaze / Coursing Houndcall / Monarch's Writ / Long Pursuit), valid mantle-as-body, or false positives. FIX: the_hollow_crown slot weapon->helm + special_effect null + removed its WEAPON_MOVES mage kit in BOTH app.js and backend/src/pvp/combat-core.js. The IDENTICAL kit (arcanebolt/immolate/cataclysm/siphon) already lives on the_monarchs_writ (same boss) so mages lose NO capability. Also moved Alpha's Mantle + Roadworn Mantle body->cape (owner: keep mantles as cape);
Mantle of the Shadow Sovereign LEFT in body (torso piece + sovereign_regalia set member whose body/plate slot is load-bearing for the W495 3pc cap). LIVE SIDE-EFFECT (owner-accepted): anyone wielding Hollow Crown as a WEAPON gets it un-slotted from weapon (keeps stats;
kit reverts to unarmed until a real weapon is equipped);
best B mage weapon becomes the_monarchs_writ. Backend line removed in source;
a deploy to drop the dead entry is OPTIONAL/non-urgent (a helm can't equip as a weapon, so the deployed backend never looks it up). [W526 below.] W526 — PER-USER RETENTION TRACKING (backend, owner-requested: real D1/D7/D30 independent of Apple's opt-in analytics). New D1 table app_opens (migration 0027;
one row per user per UTC day, UPSERT on (user_id,date_utc), composite PK = the unique constraint AND the index;
cohort/first_seen DERIVED as MIN(date_utc) per user — no users-table change, clean DROP rollback). Authed FIRE-AND-FORGET ingest POST /v1/users/me/app-open (RL_APP_OPEN ns1020, single UPSERT, no reads on hot path, date_utc server-derived). Admin-only SECRET-GATED GET /v1/admin/retention (ADMIN_METRICS_SECRET, fail-closed) → cohort D1/D7/D14/D30 EXACT + WINDOWED with maturity-correct users-weighted roll-up. account-delete cascades app_opens. CLIENT: Auth.reportAppOpen() (auth.js, _stubGate-guarded, 5-min throttle, keepalive) fired on launch (deferred 1.5s) + visibilitychange foreground (outside the audio-ctx guard). [W525 below.] W525 — FIX (owner-reported, device-specific): on the Items/Relic Archive, owned relic cards were "cut off on the right" with "the middle item so much larger" on an iPhone 16 Pro Max (fine on the owner's 14 Pro Max). NOT a per-device or breakpoint issue (both phones are ≥400px, same media regime) — it's the classic CSS-grid 1fr min-content trap: .pokedex-grid used grid-template-columns:repeat(3,1fr), i.e. repeat(3,minmax(auto,1fr)), so a column can't shrink below its content's min-width. An owned card carries a wide unshrinkable inline-flex element (the "▼ -18 vs Equipped" .pdx-delta pill / the "SELL +900" .market-trade button);
on a narrower EFFECTIVE viewport (Display Zoom / larger Dynamic Type on the 16PM) that pill exceeds the tight column and balloons it, squeezing the other two and pushing the third off-screen. Fix (CSS-only): repeat(3,minmax(0,1fr)) + min-width:0 on .pokedex-cell/.pokedex-card → 3 truly-equal columns at any width, content clips instead of overflowing. Reproduced + verified in-preview: a wide probe took a 1fr column 96px→149px (others squeezed to 69px);
with the fix it held 96px/96px/96px. [W524 below.] W524 — FIX (owner-reported): the Ascent tower icon on the Status "THE ARENA" card poked OUT the top of its tile. Cause: _aoTowerHtml sizes its box at h=44 but its rung height math (segH = h/segs - gap) ignores each of the 14 rungs' 1px borders (~28px unaccounted), so the content renders ~72px and (justify-end) spills out the top — measured 14px above the tile. Fix is CSS-only + card-local (the header tower h=92 has room, so the shared _aoTowerHtml is untouched): .sc-am-tower is now a fixed 26×46 overflow:hidden box and the inner .ao-tower is scaled 0.62 from bottom-center → the WHOLE tower fits at its intended ~44px size (verified: was 14px above the tile, now 15px inside). [W523 below.] W523 — ONBOARDING legibility: permission-aware copy for read-only HealthKit habits (Sleep). The bare "Awaiting Apple Health sync" + an empty circle read as a STUCK / uncompletable vow to a new user (the re-grade's #1 session-1 friction) — worst pre-grant or on web, where it never resolves. The auto-verification stays intentional + principled (can't fake "Sleep 7h" — must come from HealthKit);
only the WAITING-state copy changes, in _habitProgressInfo's 'awaiting' branch (~24689), reusing the proven Health.isAvailable()/permissionStatus() pattern (~36748): web/non-iOS → "Auto-tracked via Apple Health";
native, not granted → "Connect Apple Health to track" (actionable);
granted, syncing → "Syncing from Apple Health…" (reassuring, completes itself). No tap-behavior / auto-verify change. [W522 below.] W522 — HABIT→POWER THREAD, home-screen end (the W518 keystone's gated follow-on, greenlit after the owner confirmed the Ascent breakdown reads well on device). Added a single quiet connector line under the Status "THE ARENA" card tiles (_scArenaMergedHtml): "✦ Powered by your stats + relics — earned through your vows". The stat radar sits directly above this card, so the line ties discipline → the combat below, making the OSRS "habits are the engine" loop legible at the entry point (the tappable POWER breakdown inside the Ascent is the full explainer). Static caption, no new interactive surface;
CSS .sc-am-thread. [W521 below.] W521 — FIX W518 layering bug (owner-reported): tapping POWER inside the Ascent showed NO breakdown, but the card appeared on the MAIN page after closing the Ascent. Cause: _arOpenPowerBreakdown reuses showNoticeCard → .notice-card-wrap at z-index 200, which sits UNDER the Ascent overlay (.ar-overlay z 12300) — so the card rendered behind the tower and only became visible (tap-to-dismiss, no auto-timeout) once the overlay closed. Fix (CSS only): raised .notice-card-wrap z-index 200 → 12500, above the arena + its own info modals (#ct-overlay 12400 / .ct-modal 12401, the "tips" sheet sibling), below the one-time Ascent cinematic (99000) + corner toasts (99999). A notice card is a blocking must-read → belongs in the top tier wherever it fires;
also closes the latent bug for ANY notice card shown over the arena. (W520 was test-only, no app bump → tag jumps w519→w521.) [W519 below.] W519 — finish the sticky-toast sweep: the Settings "Morning reminder turned off" confirmation (~46358) was the last { sticky:true } toast (never auto-cleared, trailed across tabs). Now { duration:3000 }. ZERO sticky toasts remain in the app (W516 did the two "Reminders are off" ones). [W518 below.] W518 — HABIT→POWER THREAD (legibility keystone;
OSRS vision "habits are the engine"). The Ascent header's POWER number is the END of the chain vows→stats+relics→power, shown with zero provenance. Made the .al-power block tappable (data-ar="powerinfo" + a ⓘ affordance mirroring the LIVES "?" tips button) → _arOpenPowerBreakdown() opens a notice card: "YOUR POWER · {total}" + "{X} Stats — raised every time you complete a vow" + "{Y} Relics — gear bought with souls you earn from vows" + "Keep your vows. Stronger stats and better relics carry you higher." Numbers are EXACT + additive: _arenaCombatProfile is a linear weighted stat sum and each stat = level(vows)+gear(relics), so stats-power + relics-power = total;
relics derived as total−stats to guarantee the sum. Dispatch case added after 'tips';
reuses showNoticeCard (bodyHtml) — no new modal. CSS .al-power-i + .pwr-bd*. This unifies the habit loop and the RPG without merging any system (respects the settled Dungeon≠Ascent decision). [W517 below.] W517 — CRAFT: revive dead breadcrumb telemetry. The re-grade flagged _addBreadcrumb as called 15× but NEVER defined (all guarded `_addBreadcrumb && _addBreadcrumb(...)` → permanent silent no-ops in the first-vow / add-habits flows) — telemetry a maintainer would wrongly trust. The codebase already documents the correct recorder (W201 comment in save(): the bounded, debug-exported store is _addHabitBreadcrumb(step,data), used in ~54 other sites). Fix = renamed the 15 dead `_addBreadcrumb` call sites to `_addHabitBreadcrumb` (kept the defensive && guard;
signature identical), so those breadcrumbs now actually record. Debug-only (no user-facing/economy change;
recorder is a capped ring buffer surfaced only under hb_debug). Updated the W201 comment to match. NOTE on _pvpExit (the other audit-flagged "bug"): VERIFIED defensible, NOT fixed — `let ok=true;
try{ok=confirm(...)}catch{}` defaults to honoring the user's EXPLICIT exit tap if confirm is unavailable;
Capacitor iOS implements window.confirm (shows the dialog) so it doesn't throw in practice, and flipping the default to false would break EXIT for everyone on a confirm-less webview (trap them in the match). Left as-is by design. [W516 below.] W516 — FIRST-FRAME CALM (Onboarding legibility;
OSRS reframe — sequence, don't delete). The re-grade's #1 holistic bounce: a brand-new user lands on Habits and gets THREE+ overlapping interruptions on one frame (FA coachmark + "List View" tip + a STICKY "Reminders are off" toast). Investigated + adversarially reviewed (workflow first-frame-calm;
the critic caught a would-be regression — the listview-hint guard must test the 'hidden' CLASS not the .hidden property, else it kills the hint forever). Three surgical changes (no new machinery, nothing deleted): (1) the onboarding "Reminders are off" toast (~44320) — was {sticky:true} firing on WEB with meaningless iOS copy + never-clearing + trailing all 7 tabs + overlapping the Add Habits CTA — now native-gated (inline Capacitor check, same as the recover path ~34232;
web=no-op) + {duration:3500} auto-dismiss;
(2) the recover-path "Reminders are off" toast (~34275, native-only) — same {sticky:true}→{duration:3500} so it can't trail tabs on day-2 launches;
(3) listview-hint (_maybeShowListViewHint ~26451) — defer while the FA coachmark overlay or the W504 #first-vow-pointer is live (classList.contains('hidden') check), eager-set key preserved so it resurfaces on a later render. W504 pointer + W509 de-confliction untouched (guardrail). Out of scope (flagged, not bundled): the Settings "Morning reminder turned off" toast (~46308) is also sticky. [W515 below.] W515 — FIX W514 over-correction (owner: "now there's two progress bars"). W514 added a routine spine WITH its own banked-XP progress bar, stacked right under the existing "Seal your vows" completion bar → two bars, two denominators, confusing (a legibility REGRESSION, the opposite of the goal). Fix: dropped the second bar + the redundant pack count entirely. The only NEW, non-duplicate signal is the compound REWARD, so it now renders as a single quiet gold CAPTION folded INTO the vows header (#vows-header-reward, placeholder added in _renderVowsHeader after the bar), under the existing completion bar: "Complete your routine for +N XP" → "+X banked · +N when your routine completes" → "Routine complete — +N XP banked today". One bar, one caption. Renamed _renderHabitsRoutineSpine → _renderVowsCompoundReward (text-only;
computes fullXP/banked from _compoundFullBonusToday/_customFullBonusToday/_compoundPartialGiven;
self-hides when fullXP<=0). _activeCompoundRoutine unchanged. Wired the same 2 call sites (renderHabits after updateProgress;
renderCompoundProgress tail for live toggle updates). [W514 below.] W514 — HABIT-LOOP SPINE (legibility, OSRS reframe — keep all scope, make it clear). The Compound Effect is ~90% of rank XP but its meter only lived in the Status today-strip MODAL;
on the Habits tab, where vows are actually completed, the rank engine was invisible. Added a slim READ-ONLY "routine spine" (#habits-routine-spine) under the "Seal your vows" header that mirrors the active routine's compound projection: pack/routine name + done/total + the live "+N XP when complete" → "+X banked · +N when complete" → "✓ Complete / +N earned today" meter (reuses _compoundXpMeterHtml, which returns '' when fullXP<=0 so the spine self-hides for no-routine users). NO interactive controls (the full tappable rows stay in the modal → no duplicate ids/handlers). _activeCompoundRoutine() mirrors renderCompoundProgress's pack-selection (custom supersedes packs;
Morning⊂Locked-In phantom guard) so spine + modal never disagree. Wired from renderHabits (after updateProgress, all branches — self-hides on empty/rest-day) AND the tail of renderCompoundProgress (live toggle updates). Does NOT touch the deliberately compact-on-launch header (1z.179). [W513 below.] W513 — Arena navigation cleanup (owner). (1) Removed the redundant in-tower 'RANKED PVP — Enter the Arena' banner from the Ascent tower render (dropped _pvpTowerEntryHtml() from the _arSet tower HTML at ~8713);
PvP is now entered ONLY from the Status card's RANKED PVP/DUEL tile (#sc-pvp-btn → openArenaPvP). _pvpTowerEntryHtml left defined (repaint is a harmless no-op with no .arena-entry in the DOM). (2) EXIT from any PvP view now returns to the STATUS page, not the Ascent tower: _pvpExit's tail changed from _arRenderTower() → closeArena() (hides the #arena-overlay → reveals the Status tab the user came from). (3) Consistency: the transient PvP sub-screens (searching/creating/joining) back button no longer says 'Back to the Tower' (data-ar='tower') — now always 'Back to the Arena' (data-ar='pvplobby' → _pvpOpenLobby), so NO PvP screen routes to the tower. Sub-views (ladder/history) keep their own in-content 'Back to the Arena' → lobby buttons. Browser-verified: tower shows no .arena-entry banner;
opening PvP from the Status DUEL tile then tapping EXIT closes the overlay back to the Hunter Profile (Status), not the tower;
no console errors. [W512 below.] W512 — surface earned STREAK SHIELDS on the Habits header (owner: shields were only visible buried in the Routine Progress modal — wanted them seen next to 'Seal your vows'/Manage). Added a gold shield pill (🛡️ N) in a new .vows-header-actions group beside the Manage button in _renderVowsHeader, shown ONLY when streakShields>0 (no clutter at zero;
simple-UX). data-shield-info='global' routes the tap to openShieldInfoModal via the existing global delegated [data-shield-info] handler (no new listener). Matches the existing 🛡️ N chip style (count only). styles.css: .vows-header-actions flex group + .vows-shield-pill (gold border/glow, var(--gold)). Browser-verified: pill renders '🛡️ 2' beside Manage under Seal Your Vows, tap opens the shield modal, no console errors, hidden at 0 shields. [W511 below.] W511 — ASCENT summit-approach plateau (live balance). Rendiesel (A-rank, ~283 raw / 2829 displayed power) banked his way to F99 despite being far under the floor requirement, prompting the owner to flag floors 90-100 as too soft. Investigation (W510 un-blinded the sim first): the climb is a BUILD-power check decoupled from rank BY DESIGN (no rank gate;
bands' S/A labels are cosmetic;
stats cap at lvl20 so power saturates on gear well before S-rank's ~3mo grind) — so an A-rank summit is expected, NOT a bug. F100 itself already HARD-WALLS a 283-power build at ~0% (he is stuck at 99, per his profile) and CANNOT be raised without bricking the legit weak-weapon maxed build (Duskforge BiS is only 59% at F100;
the F100 build-power SPREAD is 491→578 and one wall can't straddle it). The REAL leak was the regular approach floors 91-99 sitting ~342-374 raw — a deep dip below the F90(399)/F100(428) boss spikes — letting a sub-power build cheaply GRIND the easy regular steps to the summit's doorstep via the patience mechanic (free wins, 2 losses/day, floors banked permanently). FIX: _ASCENT_SUMMIT_PLATEAU ×1.14 on regular floors 91-99 only (boss floors incl. F90/F100 + everything ≤90 are byte-UNCHANGED), raising them into a high plateau ~390-426 raw with F99(426) staying under F100(428). Sim-verified (Arena.simAscent, now invest-aware via W510): a 283-raw build's easiest grind step F93 drops 54%→8% and the rest go 0-4% (grind ~5-7× slower, effectively gated), a mid 385-raw build is challenged-not-bricked (50-90%), and every legit maxed build still cruises (Duskforge BiS 79-100% on the approach, the tightest legit point F95=79%;
F100 unchanged at 59/87/97%). No progress loss (already-banked floors are kept;
only future attempts at uncleared floors get harder). NOTE: this slows but does not HARD-stop determined grinding — the deeper lever for that is the banking mechanic (lose-a-floor on defeat in the top band), offered to the owner as a separate decision. [W509 below.] W509 — path-to-B+ batch 2, task 5/5 (hardens Onboarding B+): DE-CONFLICT the FA first-vow COACHMARK and the W504 first-vow POINTER banner. On the main pack onboarding path, _completeOnboardingFinish fires BOTH showFirstVowCoachmark() (a full First-Awakened overlay) AND, via renderHabits→_maybeShowFirstVowPointer, the W504 gold banner — and BOTH say 'seal your first vow', a redundant double-cue. Fix: the moment the coachmark COMMITS to showing (its hb_tour_first_vow_v1 self-gate already passed) it calls _dismissFirstVowPointer(false) — sets the session flag _firstVowPointerDismissed (so the banner's self-healing re-insert early-returns for the rest of the session) AND removes any already-rendered banner DOM, WITHOUT persisting hb_first_vow_pointer_seen. Session-only by design: the flag resets next launch, so the banner stays a SAFETY NET on coachmark-LESS paths (custom/Forge-Your-Own First Vow, reset re-entry, or any launch where the coachmark self-gates off as already-seen). Order-robust: whichever of coachmark/banner renders first, the flag-set + DOM-removal collapse it to a single cue (the coachmark overlay covers the screen regardless). One line, no new state, no economy/persistence change. node --check OK. [W508 below.] W508 — path-to-B+ batch 2, task 4/5 (hardens Economy B+): SURFACE THE W485 DIFFICULTY TILT. The custom compound is ~90% of rank XP and scales by the routine's AVERAGE habit difficulty (W485), but that lever was INVISIBLE at create-time — the W489 difficulty picker only showed per-completion XP (Easy +1 / Medium +3 / Hard +5), never the compound effect, so the Economy read as 'attendance, not effort' even though effort IS rewarded. Added a second muted help line under the create-habit difficulty picker (#custom-diff-mult, reuses .edit-diff-help — NO new CSS) reading 'Daily Compound (routine avg) — all-Easy 0.7×, Medium 1.0× base, all-Hard 1.3×'. The three values are computed LIVE in _paintCustomDiffMult() from AwakenedEconomy.customCompoundDifficultyFactor(DIFFICULTY[d].pts) — the SAME function the award path uses — so the hint can never disagree with what actually pays, and auto-tracks any future constant change. Honest framing ('routine avg', not 'this habit') since the multiplier is a routine-wide property. Painted from _renderCustomDiff right after setActiveDiff (every modal open), self-guarded so a missing AwakenedEconomy (W480 fail-mode) just blanks the line. Pure DISPLAY — awards no XP, sets no state (verified: node --check OK, line renders 0.7×/1.0×/1.3× exactly). [W507 below.] W507 — path-to-B+ batch 2, task 3/5 (Craft B→B+): PERSISTENCE-FAILURE VISIBILITY + a CI spec-count FLOOR. W491 un-blinded the critical LOAD path + 4 writes;
this extends the same _logSwallow instrumentation to 31 STATE-BEARING setItem catches that were silently swallowing (equipped gear / arena+PvP state / hunter build / leaderboard cache / the PAE anti-exploit nonce maps / paid avatar-skin / co-op award+engage credit / first-verified / boss progression / the CloudSync device-id+last-change+last-backup timestamps / onboarding-first-XP / auto-completions). Each silent failure = real data/economy loss that left NO trace;
now they surface in the W487 CI's debug log. ZERO production behavior change: _logSwallow is a no-op unless localStorage hb_debug=1. Cosmetic seen/hint/collapsed/prompted UI flags deliberately LEFT (a lost one just re-shows a coachmark). Applied via a whitelist+clean-shape transformer (the catch must directly guard the setItem) so no broad-block catch was mis-touched;
node --check OK. SECOND deliverable: a CI 'E2E spec-count floor' step (.github/workflows/ci.yml checks job, FLOOR=28) that fails the build if tests/e2e/*.spec.ts drops below 28 top-level test() calls — so the W500 smoke suite (the monolith's behavioral safety net) can't silently SHRINK (a smaller-but-passing suite still goes green). [W506 below.] W506 — path-to-B+ batch 2, task 2/5 (Scope B→B+): TWO new MID-game co-op equipment sets, activating set-chase content below the existing endgame sets. The only two viable multi-slot natural sets in the un-setted catalog (the other 3 co-op boss-lines are weapon-only → can never form a 2pc): SUNDERED CHOIR (B-rank duo: Choirmaster's Vestment/body + Discordant Crown/helm + Sundered Grips/gloves + Antiphon Striders/boots → 4 DISTINCT build slots plate+helm+gloves+boots, true 4pc;
INT/FOCUS caster archetype;
2pc +3INT+2FOCUS / 4pc +5INT+4FOCUS+2WILL) and THE GAUNT WARDENS (C-rank duo: Warden's Vigil/body + Famished Circlet/helm + Gaunt Mantle/cape + Stairwalker's Treads/boots → plate+helm+cape+boots, true 4pc;
FOCUS/VIT endurance archetype;
2pc +3FOCUS+2VIT / 4pc +5FOCUS+4VIT+2INT). PURE ADDITIVE DATA: 2 SET_BONUSES entries + tagged the 8 relics' set_id (block-scoped, all were set_id:null) — power folds through the UNCHANGED W495 _activeSetBonuses→_aggregateBuildBonuses read-point (Ascent + PvP inherit), the set-bonus panel renders the equipped set dynamically, and card-detail already shows "◈ <Set>" membership for any tagged relic. BALANCE-SAFE BY DESIGN: each is assembled from a SINGLE boss line (the easiest full sets in the game), so both 4pc bonuses are cp11 — strictly BELOW Wakeful 4pc (cp15) and Sovereign 3pc (cp17), i.e. they CANNOT raise the build power ceiling. For endgame builds they are a pure downgrade (4 B/C slots for a smaller bonus vs best-in-slot S/A), so nobody equips them over optimal gear;
they only give B/C co-op players a reason to keep + complete relic sets they'd otherwise shard. Verified: node --check OK, both sets resolve to 4 distinct typed build-slots (4pc reachable), no hardcoded set registry to update. [W505 below.] W505 — path-to-B+ batch 2, task 1/5 (IA B→B+): Items-tab first-entry coachmark. Stats + Quests tabs already fire a one-time First-Awakened coachmark on first open;
the Items tab (Relic Archive + the W502-labeled souls Marketplace) did NOT, leaving 1 of 3 non-obvious deep tabs un-oriented. Added FA_ITEMS_BEATS + showItemsCoachmark() (verbatim mirror of showStatsCoachmark;
own key hb_tour_items_v1) fired deferred from the existing  branch after renderPokedex. Teaches the two faces of the tab (Archive feeds the build;
souls buy/sell relics here). Reuses the proven _faRunCoachmark engine — collision-checked (no prior showItemsCoachmark/hb_tour_items key). [W504 below.] W504 — path-to-B+ task 5/5 (Onboarding B→B+): FIRST-VOW POINTER. A brand-new hunter lands on a dashboard full of habits with NO cue to take the first concrete action, so the +50 "First Step" payoff sits behind an unguided tap. A one-time gold pointer banner now sits atop the Habits tab ("Seal your first vow — tap a habit below to complete it. Your first Mark (+50 XP) awaits") and retires the instant they seal their first one. Gated TIGHT to a genuine first-timer: ZERO rank points (also true-falses a HealthKit auto-verified first habit), onboarding done, has active habits, none completed today. SELF-HEALING: _maybeShowFirstVowPointer (called from renderHabits' main AND fingerprint-bail paths) re-inserts the banner at the TOP of the Habits tab-panel if a habit-list re-render wipes it — no localStorage gate on CREATION (the earlier "set seen on create" deleted it on the very next render). Retired on first completion via toggleHabit→_dismissFirstVowPointer (session flag) and the 0-points gate (forever once any XP is earned);
an explicit ✕ persists (hb_first_vow_pointer_seen). Browser-verified: shows for a fresh user, survives re-renders, dismisses on the first seal. [W503 below.] W503 — path-to-B+ task 4/5 (Economy). FINDING: the re-grade's two Economy levers BOTH fight documented, intentional design — (a) the pack-difficulty-tilt is explicitly excluded in lib/economy.js ("preset packs are curated-fixed and per-habit XP already rewards difficulty") and, applied to FIXED-composition packs, would only re-scale the TUNED Morning/Locked-In rank pace (no effort-CHOICE to reward) → SKIPPED to protect the tuned economy;
(b) the "co-op B-rares out-power A-rares" cross-rank inversion is the DOCUMENTED co-op-runs-hot carve-out (duo-boss drops reward harder content), not a bug. What WAS real + safe: Band of the Roadkeeper (A-rare ring, wakeful_vigil set piece) sat at the A-cohort FLOOR (cp21, str1/vit7/int2/focus6/will5) — the weakest A-rare AND a weak set piece. Lifted to cp24 (upper A cohort: str1/vit8/int2/focus7/will6) so the A-rank set ring isn't out-powered by STANDARD B-rares + pulls its weight in the endurance set. Buff-only (no nerf to the hot co-op drops). Also taught tools/balance/analyze.py to EXEMPT co-op items from the cross-rank-inversion check (they're an intentional carve-out) so the balance record stops false-flagging them — cross-rank section now clean. tools/balance re-extracted + AFTER.txt regenerated;
30 economy unit tests still green. CONCLUSION: Economy is genuinely solid (two-tier gap closed W479, souls sink W494, set bonuses W495, difficulty tilt W485) — its remaining "attendance-vs-effort" gap is largely by-design;
this is the safe refinement, not a tuned-pace gamble. [W502 below.] W502 — path-to-B+ task 3/5 (IA, toward B+): two findability wins. The re-grade's headline IA task ("move Souls/World-Rank/Kill-Log onto Status") was a MISREAD — those live in the GLOBAL app header (visible on every tab, not Habits-specific) and the owner DELIBERATELY boots that header COMPACT (1z.179) so content claims the screen;
forcing them onto Status would fight that + the simple-UX preference, so it was SKIPPED. Instead did the two real, owner-aligned gaps: (1) ARENA HUB — Ladder + Match History were faint tertiary <a> text links (easy to miss as ranked-progression destinations);
promoted to two first-class labeled buttons (.pha-dest, icon + label + chevron). Same data-ar (pvpladder/pvphistory) → handlers unchanged. (2) MARKETPLACE LABEL — souls-spending had no labeled home (only inline BUY/SELL chips on relics), so a new player had no "where do I spend souls" answer;
added a one-line .pokedex-market affordance to the Items-tab header ("◈ Marketplace — spend souls to buy relics here, or sell duplicates back"). Browser-verified. NOTE: IA's remaining anchor is the 7-tab bar vs the iOS 5-tab convention (Status/Stats/History overlap) — a larger, riskier restructure deferred (not weekend-safe). [W501 below.] W501 — path-to-B+ task 2/5 (Core Loop B→B+): LIVE COMPOUND XP METER. The Compound Effect is ~90% of rank XP but was invisible mid-day — the routine-progress row showed only a done/total count, so a player couldn't see how much XP they were working toward or how close they were. _compoundXpMeterHtml() adds a thin meter to every pack row + the custom "Your Routine" row that fills with the XP BANKED so far toward the FULL projected compound XP this day will pay ("Finish the routine for +N XP" → "+X banked · +N when complete" → "+N XP earned today"). Pure read: reuses the EXACT award-path helpers (_compoundFullBonusToday / _customFullBonusToday / _compoundPartialGiven) so the displayed number can never disagree with what actually pays;
recomputed on every renderCompoundProgress (no caching → survives mid-day routine edits / pack flips). SILENT — no popup, no sound (preserves the no-flood win). Renders in the Routine Progress modal body + the (CSS-hidden) footer strip. Browser-verified (3-habit custom routine → meter shows projected XP + 0% fill). [W500 = e2e suite greened + CI gate flipped to blocking;
test/CI only, no app bump.] W499 — owner spacing tweak on the W497 Arena Hub: the RANKED·/FRIENDS· group headings read too close to the button beneath them (a W497 regression — `.pha-grouplabel + .pha-btn{margin-top:0}` collapsed the gap to ~1px). Now 16px below each heading (vs the 9px inter-button gap) + ~14px above the FRIENDS label to separate the two groups. CSS-only (.pha-grouplabel margin + the label→button margin);
note the label margin-bottom and button margin-top COLLAPSE (.pvp-hub-actions is block), so the gap is driven by the button's margin-top. Browser-verified at 390x844. Rides MacBook build #361. [W498 below.] W498 — two device bugs the owner caught on the W497 Arena Hub. (1) CREST COLOUR: Platinum rendered BLUE — but blue reads as Diamond. Swapped the platinum<->diamond colour sets (the _PVP_EMBLEM_SVG crests, _PVP_TIER_PAL c/deep/rim, and the VS aura tint) so Platinum is now GREEN (#5eead4 family) and Diamond is BLUE (#7dd3fc family);
each tier keeps its own ornament (platinum wings / diamond gem-facets), only the colour moved. Scoped per-line (the greens like #5eead4 are the app-wide teal accent — a global replace would wreck the UI). (2) "RANKED AWAKENS SOON" sat in-flow at the bottom of the hub on scroll instead of popping only on Find Match. ROOT CAUSE: `.pvp-hub > *:not(.pvp-hub-aura){position:relative}` (the lift-content-above-the-aura rule) silently overrode the W433 sheet's intended position:absolute, dropping it into normal flow. FIX: exclude the scrim+sheet from that rule AND make them position:FIXED (they pin to the fixed .ar-overlay phone-frame — which has backdrop-filter, so it's the containing block;
no transform issues). Now the closed sheet sits BELOW the frame (hidden even when scrolled to the page bottom) and only the Find Match tap slides it up as a bottom-sheet modal. Browser-verified at 390x844: closed sheet below-viewport/not-visible-on-scroll, open sheet pinned to the frame bottom (466-844), Platinum crest green. Rides MacBook build #361 with W494-W497. [W497 below.] W497 — ClaudeDesign "Arena Hub — Button System Redesign" (handoff #8). Collapses the PvP hub's green/gold/grey button patchwork into ONE shell (.pha-btn) with four ranked variants, reordered by what a hunter actually DOES. The old layout made the one UNAVAILABLE action (gold .pvp-hub-find "Find Match") the LOUDEST;
this flips it — priority is fill weight, not a new colour per button. Grouped under two micro-labels: RANKED·Affects ELO = (1) "Spar an Echo" .pha-btn--primary (the only RATED mode playable now → loud filled teal, "Rated" tag, Enter ›) + (2) "Find Match" .pha-btn--locked (real ranked queue, muted panel + gold 🔒 SOON, opens the coming-soon sheet);
FRIENDS·Casual = (3) "Spar a Friend's Echo" .pha-btn--outline (unrated teal-outline) + (4) "Duel a Friend" .pha-btn--ghost (neutral share-a-code). _pvpHubActionsHtml() rewritten;
SAME data-ar values (pvpfind/pvpsoon/pvpfriendecho/pvpfriend) → all delegated handlers byte-unchanged. New _PVP_ECHO_SIG_CC (currentColor echo glyph) so the icon inks correctly per variant (dark on the filled primary, teal on the outline). Handoff's Change #2 (float the "Ranked awakens soon" message into a modal) was ALREADY DONE by W433 — `.pvp-hub-scrim`/`.pvp-hub-sheet` are hidden-by-default absolute overlays (opacity:0 / translateY(110%)) that open only on the Find Match tap;
verified still works with the new button (scrim+sheet .on). Old .pvp-hub-echo/-find/-friend/-friendecho CSS now orphaned. Browser-verified (4 variants + 2 group labels render, no console errors, sheet toggles). Rides MacBook build #360 with W494-W496. [W496 below.] W496 — ClaudeDesign "Merged Ascent + PvP Button" (Direction D), owner-requested ("I was not a fan of two buttons. I decided to make it together"). The two stacked home-screen buttons — the big W255 Ascent banner (.sc-climb / _scClimbStripHtml) + the W488 Ranked-PvP row (.sc-pvp-btn) — are FUSED into one "THE ARENA" umbrella card (gold→teal flanking rules) holding two independently-tappable inset tiles: a gold ASCENT tile (floor-accurate _aoTowerHtml tower + "N /100" + CLIMB ›) and a teal RANKED PVP tile (crossed-swords glyph + "Duel" + FIGHT ›). New _scArenaMergedHtml() renders it;
REUSES the existing #sc-arena-btn / #sc-pvp-btn ids so the delegated openArena / openArenaPvP handlers are byte-unchanged (a tap on nested tile content bubbles to closest('#…') — browser-verified both resolve). Tile bonus over the static mock: the tower reads LIVE ascent state (mock hardcoded 7 lit). Tradeoff: the compact tile drops the floor BAND name ("The Stormcrown") the old banner showed — per the mock's design;
the band still shows inside the Arena/tower view. _scClimbStripHtml + the old .sc-climb/.sc-pvp-* CSS are now orphaned (left in place, harmless). Browser-verified (boot clean, both tiles + header render, floor 64 correct, taps delegate right). Rides the SAME unbuilt MacBook build #360 as W494+W495 (owner: "before I push that, implement this"). [W494+W495 below.] W494+W495 — TWO economy-depth features (path-to-A re-grade #2 queue, items #3 + #5), shipped together after a 4-critic adversarial review (verdict ship-with-fixes;
balance ruling = no constants trimmed). W494 RELIC UPGRADE (the terminal souls sink the economy lacked): spend souls to raise an OWNED relic's upgrade_level 0→3, each level +1 to that relic's SINGLE DOMINANT base stat only (_relicDominantStatKey). Cost = RELIC_UPGRADE_BASE_COST[tier]×2^level (E300…S3200;
maxing an S relic = 3200+6400+12800 = 22,400 souls). Bounded by design: a fully-maxed 8-relic build gains only ~+24–38 combat power (<10%), so it can't bust the F100 ceiling or PvP symmetry. SUNK-COST: relicSellPrice ignores upgrades + selling the LAST copy resets the level → zero arbitrage. RARITY-GATED to tradeable relics (rare/ultra/mythic), mirroring the market (commons excluded). upgrade_level rides hb_inventory (CloudSync 'inventory', zero backend change). Combat hook = ONE line in _aggregateBuildBonuses, so Ascent (_ascPlayerPower) + PvP inherit it automatically. UI: a card-detail UPGRADE row (level pips, next-cost, +1 STAT, MAX state). W495 SET BONUSES (activates the long-dead set_id schema — 0 prior functional consumers): SET_BONUSES table — Sovereign's Regalia 2pc{vit4,will3}/3pc{str4,vit6,will4,focus3} (NO 4pc: body+legs both collapse to the one 'plate' slot, so 3 is the real cap), Wakeful Vigil 2pc{vit3,focus3}/4pc{vit6,focus5,will4}. _activeSetBonuses counts equipped pieces per set over the live build and applies ONLY the single highest qualifying tier (cumulative-total, no 2pc+3pc double-count — browser-verified: a 3pc Sovereign build totalled 136 = gear 116 + upgrade 3 + 3pc 17, NOT 143). Folds into the SAME _aggregateBuildBonuses read-point. These set pieces ARE best-in-slot, so the bonus is a flat endgame-collection power bump by design (not an opportunity-cost trade) — fine because F100 is a fixed wall already cleared at 84%/65% and PvP is server-authoritative + symmetric. UI: a SET BONUSES progress block under the equipment-bonuses grid (tiers lit when active) + a set-membership discovery tag in the card-detail modal. REVIEW MUST-FIXES (both data-loss on the now-paid inventory blob): (1) CloudSync startup push is now guarded — a provably-STALE device (local last-change older than the cloud snapshot) no longer blind-overwrites a newer cloud copy via last-writer-wins (only SKIPS when older, never blocks a newer-local push, so no new-change loss;
full field-merge still the v2 item);
(2) a CORRUPT hb_inventory no longer auto-stubs+persists+pushes the wipe — loadInventory runs the session on an in-memory stub WITHOUT persisting, preserves the raw blob for cloud-restore, and collectState SKIPS the inventory key while flagged (_inventoryCorrupt) so another subsystem's push can't clobber the cloud's good copy. Browser-verified end-to-end (boot clean, cost curve 3200/6400/12800, max cap, exact 22,400 debit, rarity gate, both combat folds). [W493 = Status tap-affordances;
below.] W493 — finish the Status tap-affordances (IA #2 major, started in W490): (1) the AVATAR (an <img>, role=button "tap to change your look") now carries a small ✎ pencil badge pinned to its lower-right (✎ = the app's edit verb, matching the name-edit cue) — a sibling in .sc-avatar-row, not nested, so it can't disturb the avatar crossfade;
(2) the RADAR (6 tappable stat axes -> openStatDetail, previously only cursor:pointer with no touch affordance) gets ONE faint "TAP A STAT" whisper at the bottom-center of the SVG (owner prefers a single hint to 6 per-axis cues). With W490's class-chip cue, all 3 always-tappable Status surfaces now signal it;
the plain rank-hero disc stays uncued (it's inert — only the 100k-club variant is tappable). [W492 = e2e smoke now runs in CI via cross-platform serve.mjs;
test/CI plumbing, no app bump.] W491 — error VISIBILITY (re-grade's #1 Craft/Economy leverage): routed the 9 highest-value silent catch(_) blocks through _logSwallow (no-op unless localStorage hb_debug=1 — ZERO production behavior change). Instrumented: the whole-state-wipe load() recovery (a transient parse/quota error silently reset ALL habits/points/stats), the 6 per-subsystem loaders that silently fall back to defaults (souls/inventory/bosses/equipped/hunterBuild/leaderboard — i.e. souls vanish, gear un-equips, streaks reset), the daily-cap ledger persist (W461 bug class), and the checkCustomRoutineCompound outer catch (a throw there silently drops compound XP ~90% of rank XP). Surfaces the silent data-loss/economy failures that blinded the new W487 CI. ~26 lower-value reward-integrity/secondary persist catches noted for a follow-up (cosmetic DOM/render catches deliberately left). W490 — Status-tab tap-affordance cue v1 (IA): interactive Status elements opened modals with no visual "tappable" cue. Added a shared subtle ".sc-tap-cue" ("›") + applied it to the always-tappable class chip (.sc-hero-class[data-class-key], role=button "Class details"). DELIBERATELY scoped to that one element this pass: the rank hero is only CONDITIONALLY tappable (100k-club variant;
the plain disc is inert — must NOT be cued) and the 6 radar dots are SVG-positioned — both need a careful follow-up pass rather than a rushed blind edit to the first screen. W489 — per-habit difficulty picker for custom habits (closes the W485 "effort, not attendance" gap for custom-CREATORS). Custom habits were hard-pinned to medium (CUSTOM_HABIT_DIFFICULTY), so W485's difficulty tilt was a flat 1.0x for the exact population it was named to fix. Now the Create modal has an easy/medium/hard picker (#custom-diff-row, reuses the edit modal's .diff-row/.diff-btn CSS — no new CSS);
default medium (economy byte-identical for non-users). ANTI-GAMING CLAMP: legendary is EXCLUDED in 3 layers — omitted from the markup, ignored in the click handler (_clampCustomDiff), AND clamped on persist (saveCustomHabit) — so a user-authored habit tops out at hard (5 XP/completion, 1.3x compound), never the 1.5x legendary tier. Matches the library habits' own easy→hard range (no library habit is legendary either). Edit-modal unlock deferred (create-only v1;
existing customs stay medium). W488 — discoverable Ranked PvP entry (path-to-A IA fix): ranked PvP was buried 2 layers deep behind the gold "THE ASCENT / CLIMB" banner (nothing said Duel/PvP). Added a slim, teal-accented "Duel a Hunter / RANKED PVP" button (#sc-pvp-btn) as a sibling row under the Ascent strip on the home Score screen, gated by PVP_ENABLED. New openArenaPvP() deep-links straight into the PvP hub (openArena(skipOpening=true) to un-hide the overlay + wire [data-ar] without popping the Ascent first-entry challenge over the hub, then _pvpOpenLobby()). Wired via the existing setupOriginStorySheet delegated handler. The in-overlay "Enter the Arena" banner stays (serves users already in the tower). [W487 = CI gate + glob/manifest bundling, build-pipeline only, no app bump.] W486 — teach the economy (path-to-A Onboarding). FINDING: the app ALREADY teaches it — a 7-section ClaudeDesign Field Manual (Rank·XP, Stats·Class, Bosses·Souls, …) + First-Awakened coachmarks (Quests, Stats) + tappable rank/stat/compound explainers;
the "no explanation" audit was stale. TWO real gaps fixed: (1) the COMPOUND EFFECT — ~90% of rank XP — was the one UNDOCUMENTED mechanic;
added an 8th Field Manual section "THE COMPOUND EFFECT" (+ a nested-diamond glyph) in a placeholder ClaudeDesign voice (pending their refinement — the others stay canonical, do not reword). (2) the manual was buried in Settings;
added a ONE-TIME pointer card (#fm-pointer-overlay, reuses First-Mark styling) chained after showFirstWinScreen ("New to the System? Read the manual" / "Maybe later") gated by hb_fm_pointer_seen → openFieldManual(). New users discover it at the peak teaching moment;
existing users unaffected (re-openable from Settings → Field Manual). W485 — difficulty-aware CUSTOM compound (path-to-A Economy: "rank rewards attendance, not effort"). The compound is ~90% of rank XP and was difficulty-BLIND;
now the CUSTOM compound scales by the routine's AVERAGE habit difficulty (easy 1/med 3/hard 5/legendary 10), REBALANCED around medium (3pts=1.0x, byte-identical to before — NO inflation, owner's requirement): all-easy ~0.7x, all-hard ~1.3x, clamped [0.7,1.5]. customCompoundDifficultyFactor in lib/economy.js (6 tests) threaded into _customFullBonusToday + awardCustomRoutineCompound;
computeTodayXP inherits it via the ledger;
reconciles via the W479 clawback (a mid-day difficulty change is handled exactly like a size change — verified). CUSTOM-ONLY (preset packs untouched;
per-habit XP already rewards difficulty for everyone). 4-dim multi-agent review = SHIP, no must-fix. HEADROOM: no library habit is 'legendary' + custom-CREATED habits are pinned medium, so the OPERATIVE band TODAY is ~0.7x-1.3x (all-custom routine = 1.0x);
the 1.5x ceiling is forward-looking (needs legendary habits or per-habit difficulty selection). 6 new sim-reconcile invariants (129 total) + days-to-S+ sim (tools/economy/sim-difficulty.js: medium 153d UNCHANGED, easy->hard reachable spread ~1.76x). W484 — Streak Shields are now earned from ENGAGEMENT, not routine discipline: 1 shield per 14 distinct DAYS ACTIVE (any day with >=1 completion), max 3, in a SINGLE GLOBAL pool (was: a 14-day-consecutive per-routine completion streak with a per-routine pool). Rewards just showing up. tryEarnActivityShield() (called from check() on each completion) REPLACES tryEarnShield();
idempotent + recurring via shieldMilestoneClaimed (the highest 14-days-active milestone credited), with first-run SEEDING (-1 sentinel) so a returning user's past days never retroactively flood shields, and a cap that still advances the milestone (no re-flood on spend). processStreakRollover consumes from the one global pool to forgive a missed day on ANY routine. Removed the now-moot W482 per-routine shield-earn + prevShields snapshot/refund-rollback (the custom STREAK rollback stays). Migrated persistence: hb_shields object->number (summed, clamped) + new hb_shield_milestone. Updated the 🛡️ info modal + bonus-info copy (were "per-pack 14-day streak"). Adversarial-reviewed (logic clean: migration throw/flood-safe, earn idempotent/seeded/capped);
4 new sim-reconcile invariants (123 total). W483 — co-op ALLY rank gate (owner design change): you may now only pair hunters who BOTH meet a co-op boss's rank prerequisite. Reverses the old "the invited partner can be any rank — they're just helping" design (a C-rank ally could be invited to a B-rank hunt). BACKEND (coop-boss.ts create): after the existing summoner gate, look up the PARTNER's rank_tier in public_profile_summary and reject with ALLY_RANK 403 if below cfg.rank (same RANK_ORDER incl S+, same default-E trust model). CLIENT: the ally picker now shows each friend's rank tier and LOCKS under-rank allies (non-tappable "Needs B" row, greyed) so the invite is never even offered;
_coopRankMeets mirrors the server RANK_ORDER exactly (unknown/unsynced rank => E, matching the server);
ALLY_RANK + INSUFFICIENT_RANK now map to clear picker messages. "Can't invite" (summoner) was already enforced by the summoner gate + the boss preview-gate;
this closes the "can't be invited" half. W482 — finish W479: make the custom-path compound a TRUE PEER of the preset packs (the B− re-grade's #1 finding — custom was structurally fragile for identical effort). (1) STREAK SHIELD: awardCustomRoutineCompound now earns a shield at each 14-day milestone (tryEarnShield) + snapshots prevShields;
processStreakRollover iterates a new FORGIVENESS_PACK_IDS (['morning','locked-in','custom']) so a missed custom day CONSUMES a shield / Honest-Rest and holds the streak, else breaks it — identical to a pack. (2) PERSISTENT STRIP: renderCompoundProgress shows a "Your Routine" row (Day-N + shield + Rest chips) for custom-path users (gated !_onPresetPack && >=3 habits;
suppresses the misleading partial-pack nudge;
no phantom row on a preset pack). (3) RETIRED showCustomWarning — the stale "custom gets no bonus, switch to Morning" onboarding scare (+ neutralized the path-note copy + removed the dead modal/CSS). BUGS: (4) _refundOrphanCustomCompound now neutralizes a pre-W482 stale-save streak-inflation (prevStreak-absent -> streak-1, reload-safe) + rolls back a phantom shield on a superseded milestone;
(5) diffPts/isWeekend take the COMPLETION's date so a historical/backfilled seal uses that day's weekend x2, not today's (LA-anchored). 18 new sim-reconcile invariants (115 total) prove a shielded miss preserves the custom streak + the strip state. W481 — readable centered "notice cards" for long messages that were clipping + flashing by at the screen bottom (Richie's report). Generalized the W477/W478 compound nudge into a reusable showNoticeCard (centered, tap-to-continue, no auto-dismiss, 350ms tap-guard, .notice-card CSS renamed from .compound-notice + loss/warn tone variants). Converted: co-op hunt DEFEAT (was a bare 2.2s bottom toast — the co-op WIN already gets a rich result modal), the Storage-full data-loss warning, Streak Shield earned, and Honest Rest day. Left as quick bottom toasts: all the short status floats (Link copied / X equipped / Now hunting Y / friend + backup + rank-gate confirmations), shield-USED (it staggers multiple per-pack notices on rollover), and solo boss kills (they already show a boss-result modal). W480 — CRITICAL iOS build fix: lib/economy.js (the W471 economy-math extraction that defines AwakenedEconomy, which app.js delegates ALL stat-level/XP math to) was NEVER copied into the www/ bundle by prep-local-build.sh / codemagic.yaml (both copy JS by explicit name). So on every iOS build since W471, index.html referenced lib/economy.js but the file 404'd → AwakenedEconomy undefined → statLevel() threw on the Ascent power path → POWER read 0 (display catches the throw) AND tapping FIGHT silently no-op'd ("can't enter tower", the throw propagates uncaught from the unwrapped arenaMatchup→_ascentPlayerCombatant). PvP/spar survived (server-authoritative). Fix: (1) both build scripts now copy lib/economy.js into www/lib/ + FAIL the build if it's missing;
(2) _ascentPlayerCombatant wraps the player-stat/profile build in try/catch (mirrors the display path) so a calc throw can never dead-end FIGHT again;
(3) boot logs a FATAL if AwakenedEconomy didn't load;
(4) gear bonus is numeric-coerced so a corrupt build can't poison power into NaN. W479 — custom-path compound (path-to-A Economy, the two-tier gap): a SELF-BUILT routine (matching no preset pack) now earns a daily Compound Effect on the Morning streak curve, scaled by routine size (full Morning rate at 10+ habits, CAPPED there so the curated Locked-In cycle stays premium;
>=3-habit floor). Mirrors checkCompoundEffect incl. W459 graded partial credit (so a near-complete custom day never pays zero — no re-introduced compound cliff) + the bonus celebration ("Personal Routine Bonus"). Fires ONLY when NOT on a preset pack (no double-dip);
idempotent via compoundAwarded['custom'] (uncheck/recheck can't re-award). Pure size + fractional-partial math lives in lib/economy.js (8 new node:assert tests). Closes the ~6-10x days-to-S+ gap for EQUIVALENT effort (sim: 934d->153d at 10 habits = exact pack parity;
5 habits stays proportional/fair). Tools: tools/economy/. W478 — partial-compound nudge is now TAP TO CONTINUE (no auto-dismiss): a light backdrop captures the tap, a pulsing "Tap to continue" cue, tap-handler attached immediately with a 350ms guard so the completing tap can't carry through (and it can never get stuck). W477 — partial-compound nudge is now a CENTERED, readable card (was a bottom showHabitToast whose long "+N partial <pack> bonus — finish all…" text ran off the screen edge). Fires once per pack per day;
auto-dismiss + tap to close. W476 — stat level-up toast (path-to-A Core loop): the in-between stat levels (2/3/4/6/7…) now fire ONE lightweight, non-blocking, stat-coloured toast per stat per completion ("STR reached Lv.3") instead of the full-screen modal flood (1z.276A);
milestone levels (5/10/15/20, the only ones with bonus rank XP) keep the full modal. Purely celebration cadence — no economy change. W475 — ClaudeDesign Relic Archive (Items tab) visual refresh: rarity sections are now contained cards with an enriched header (accent bar + rarity sigil + sub-label + per-section mini progress bar, per-rarity tint);
sticky blurred filter/sort bar;
tightened spacing (9px section gaps, removed the header divider + the "tap an archetype" hint). Structure/features (deltas, chips, buy/sell) were already live from W450/W470 — this is polish only. W474 — drop-table power-curve tune: retuned the 2 W306 shop weapons (Aetherspire/Wraithwind) from cp41/39 DOWN into A's band (cp33, kept shop-buyable, Wraithwind renamed "Far Hunt");
raised D-ultras 15→18 (killed the E=D flat);
lifted the S Regalia floor 34→36 (cleared the A/S tie);
flattened Alpha's Mantle E-rare 10→8;
fixed 5 stale Steel Wolf tier labels (D→E). E→S now rises monotonic, no flats, no solo outliers/inversions. Tools in tools/balance/. W471–W473 path-to-A batch 2: W471 extracted the pure XP/rank/compound/soft-cap math to lib/economy.js (17 node:assert tests, app.js delegates — zero behavior change);
W472 opt-in onboarding lore glossary (info dot on the naming screen → Hunter/Mark/Vow/System primer);
W473 _logSwallow diagnostic on 9 critical-path catch(_) (souls/bosses/inventory persistence + coop reward), no-op unless localStorage hb_debug=1. W470 — path-to-A prominence anchor: (1) Marketplace spend-sink surfaced via a "Ways to spend" CTA in the souls modal (routes to Items tab);
(2) WLT de-emphasized on the Status radar (muted slate, smaller dot/label, never the dominant axis) so it stops reading as a 6th combat stat. W469 — Perfect Day guild event: milestone perfect streaks (7/14/21/30/60/100) now post a privacy-safe public 'perfect_day' event to friends' guild feed + the user's own Hunter feed (radiant-gold ★, "sealed a 30-day Perfect streak"). Band-keyed + once-ever per band (pinned clientEventId). W465.1 — souls-farm fix hardened per adversarial review (in-memory fallback so the kill-reward guard can't fail-open under storage failure). W465 — URGENT: patch souls-farm exploit (solo boss re-killed via unguarded resolver backfill on re-engage → +50/+5 souls per app entry);
kill rewards now once per (boss,day) via an independent hb_kill_reward_ flag. [W464.1 — durable co-op drop credit (coop_boss_awards table + atomic POST /claim;
drop lands EXACTLY ONCE per user). W464.1 — adversarial-review hardening: cancel-race now carries the award (server-side), _awardCoopKill never rejects, and grants BEFORE persisting the local guard. (W463 = the 4 co-op bug fixes: join-429, false-empty dashboard, missed-drop reconcile, rank gate.)
