{
  "name": "awakened-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cloudflare Workers + D1 backend for Awakened v2.1 (Sign in with Apple + global leaderboard). See ../BACKEND.md for design contract.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "@types/node": "^22.7.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}
